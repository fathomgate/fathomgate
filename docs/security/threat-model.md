# Fathomgate threat model

Maintained by security-reviewer (`.claude/agents/security-reviewer.md`); each PR review adds or updates rows here. [SECURITY.md](../../SECURITY.md#threat-model-summary) keeps the short public summary. This file was created in the T0.17/T0.18 review (PR #53), so its catalogue holds the rows that review touched; earlier findings live in SECURITY.md and the review handoffs under `docs/handoffs/` until they are folded in.

## Assets

- The network devices behind each upstream, and their configuration.
- The human's approval: an answer to a prompt, and from M3 an approval of a held call. Only a human may give it (invariant 6).
- The agent's view of what fathomgate itself said, as opposed to what an upstream said (origin labels).
- The audit chain and its signing key (M1 onward).
- Upstream credentials the operator passes with `--upstream-env-pass` (device passwords, `SSH_AUTH_SOCK`; ADR 0017). The upstream must receive them; nothing else should.
- The repository's issue tracker, which mirrors the board: the `issues: write` token of `.github/workflows/issue-hygiene.yaml` (M1-38) can relabel, comment on and close any issue.

## Trust boundaries

| Boundary | Trusted side | Untrusted side |
| --- | --- | --- |
| Agent ↔ proxy | proxy | agent (a model; can be steered by anything it reads) |
| Proxy ↔ upstream | proxy | upstream process and everything it returns (invariant 7) |
| Proxy ↔ approver | proxy (server-side identity) | nothing the agent or upstream supplies |
| Proxy ↔ source of truth | proxy | inventory data (M2) |
| Proxy ↔ operator | operator | none; local files are owner-only |
| CI ↔ GitHub Issues (M1-38) | `issue-hygiene.yaml` and the board in `main` | issue titles, bodies and authors (anyone can open an issue on the public repository) |

## Attack catalogue

Status: mitigated (package and test), accepted (with scope), or open (with owner or board task).

| Attack | OWASP MCP | Status | Where |
| --- | --- | --- | --- |
| Progress spoofing: an upstream uses a progress `message` to put text the agent reads as fathomgate's own (a `[from fathomgate]` line, ANSI or bidi tricks, a spelled escape that a decoding reader turns into a newline followed by a fake label) | MCP03, MCP05 | Mitigated | `internal/proxy/progress.go` (`progressMessage`: `[from <server>]` label, `escapeControl`, 512-byte cap, dropped if it folds to an origin label); `sanitize.go` doubles every backslash so only fathomgate's escapes read as escapes; `label.go` decodes escape sequences before the `[from` check. Tests: `TestProgressMessage`, `TestHasOriginLabel`, `TestRelabelElicit`, `TestRelayUpstreamError`, `TestRelabelSchema`. The same path serves prompts, relayed errors and stderr. Since the PR #58 re-review (R1, R4) the fold also decodes HTML character references (numeric and the named brackets, amp, lt, gt), `\u{...}` escapes and percent-encoding, strips HTML tags and markdown punctuation, and fails closed past eight nested layers; tests `TestMarkupSpoofs`, `TestDecodeFailsClosed`, and the same inputs in `TestProgressMessage` and `TestRelabelElicit`. Residual, accepted and listed in label.go and SECURITY.md: homoglyphs outside the fold (mathematical alphanumerics, precomposed accents, other scripts), encodings it does not decode (other HTML named references, octal and named backslash escapes, quoted-printable, LaTeX, base64), and a label split across two fields. The `[from <server>]` label on fathomgate's own text is the second layer |
| Progress flood: an upstream sends notifications faster than the agent can use them | MCP05 | Mitigated | `progress.go`: per call 10 at once, then 5 per second; the newest held-back one is sent when a rate slot frees up (S4, T0.28) or before the result. Tests: `TestProgressRateLimit`, `TestProgressBucket`, `TestProgressFlushOnTimer`, `TestUntilToken` |
| Progress token scope: an upstream injects progress into another call | MCP03 | Mitigated, residual accepted | Tokens are at least 128 random bits (`crypto/rand.Text`, 26 base32 characters, 130 bits) in a per-upstream map, so an upstream cannot reach calls on another upstream. It can target another call in flight to itself only, under its own label, which is no more than it could say in that call's result. The agent's token never goes upstream. The control is the token: `watchProgress` draws it, fresh for every agent request, when it registers the mapping and redraws while it is already mapped, so a token is never mapped to two requests, and the relay writes only to its own request's session under that request's context (T0.30). The mapping keeps no owner record beyond that; the unused session key, transport and principal fields T0.30 added were removed (T0.48). Tests: `TestProgressRelay` (foreign token dropped, no mapping outlives its call), `TestProgressAfterCall`, `TestProgressRelayPerSession` (a stateful session and a stateless request over the listener and the local agent, all with the same agent token at once, each get only their own progress), `TestWatchProgressNeverSharesToken` |
| Cross-principal `requestState` replay: a holder of one listen token presents an MRTR `requestState` fathomgate issued to another principal's call (read from a shared log, a proxy, or another agent's context), or one issued over another transport, to answer or resume that call's upstream prompt | MCP07 (auth), MCP03 (confused deputy) | Mitigated (T0.30) | `internal/proxy/state.go`: `fg4.` envelopes (`ng3.` before ADR 0019) carry the transport and principal as AES-GCM additional authenticated data (`stateBinding`), supplied on open from the retry's own call, so another principal's or another transport's retry fails authentication before anything is decrypted and gets the forgery's detail (`requestState does not verify`), naming neither side; nothing reaches the upstream. `input.go` `warnState` logs the presenter's transport and principal at Warn, rate-limited, and since T0.48 also a same-principal retry whose state verifies but whose tool or arguments changed, with fathomgate's own reason and no argument or envelope value. `ng1.`, `ng2.` and `ng3.` envelopes cannot verify after an upgrade (per-process key) and get a detail that says so. `transportOf` reads a request with a principal as `http` whether or not go-sdk set its header. Tests: `TestSealerBinding`, `TestHTTPRequestStateBoundToPrincipal`, `TestRequestStateBoundToTransport`; all three fail with the binding removed from the additional data (checked, then reverted); `TestTamperedRetryLogged`, `TestTransportOfFailsClosed`. Residual, accepted: the binding is the listen token, not the client, so for 30 minutes after issue (`stateTTL`) the same state can be presented again and again by anyone holding the same token: the client it was issued to, another client sharing that token, or a stateful session of that principal presenting a state issued to one of its stateless requests. Each presentation reaches the upstream (there is no single-use id before M3), and the upstream's own state decides what a repeat means. Stdio side, closed in T0.31 (L1 in the security review of T0.48): a listener call cannot bind as the local agent's `{stdio, ""}` even if go-sdk stops copying the HTTP request into the request's `Extra`. The listener marks the context of every authenticated request, go-sdk keeps it in the session context it builds from that request, `transportOf` reads the mark as `http`, and a call that reads as `http` with no principal is refused before it is keyed or forwarded. Tests: `TestListenerCallWithoutExtra` (both eras over the real handler with `Extra` stripped by a middleware; the upstream sees nothing, and the local agent on the same proxy is unaffected; fails with the mark removed, checked, then reverted), `TestTransportOfFailsClosed` (mark cases). `fathomgate serve` also keeps the two sides exclusive (ADR 0016). A principal is attribution only, never an approver (invariant 6). Spec: profile-schema sections 8.2 and 8.4 |
| Upstream notification queue blocked by a slow agent: go-sdk dispatches an upstream's notifications and requests one at a time, so a progress write to an agent that stops reading, made on that goroutine, would stall every later message from that upstream (another call's `elicitation/create` and every later notification), and the call's end would wait on the stalled write, leaving a stale in-flight entry that breaks prompt attribution | MCP05 (availability) | Mitigated (T0.28) | `internal/proxy/progress.go`: `relay` only updates state and queues, under a mutex never held across a write; one sender goroutine per call writes, from a queue of at most 10 (newest replaced when full); the call leaves the upstream's in-flight set before `finish` waits (T1 in the review of PR #63, so a stale entry never makes another agent's prompt unattributable), and `finish` waits at most 1 s (`progressFinalWait`), then drops the rest and starts no new write. Test: `TestProgressStuckAgent` (two agents on one stateful upstream; the stuck one's flood is read in full, its call leaves the in-flight set, the other's `elicitation/create` and result flow, nothing follows the stuck agent's result once it reads again, and its sender returns); it hangs on the pre-T0.28 relay. `TestProgressQueueBounded`; `TestEndBeforeFinish` (agent B's call arrives while A's `finish` is still waiting and gets the upstream's `elicitation/create`; it fails on the old defer order). Residual, accepted: go-sdk's stdio transport cannot abandon a write in progress, so a sender stuck on a non-reading stdio agent lives until that agent reads or disconnects (one per call, holding at most one notification), and the stuck agent's own calls end up to 1 s late. Over Streamable HTTP every write has a deadline (row below). A sender still waiting for the write lock when `finish` gives up can write one notification after the result on stdio (accepted: the agent may ignore progress for a finished token); over HTTP go-sdk refuses it because the result closes the call's stream, pinned by `TestHTTPProgressNeverFollowsResult` (T2, T4) Spec: profile-schema section 8.4, "Delivery" |
| Resource exhaustion by a client that does not read: an agent on the HTTP listener stops reading its response streams (or opens calls and drops their POSTs), so writes block, progress senders pile up, and a 2025-era call whose POST was dropped keeps running upstream with no request open | MCP05 (availability) | Mitigated (T0.27; T3 in the review of PR #63) | `internal/proxy/http.go`: every write and flush runs under a write deadline (`HTTPOptions.WriteTimeout`, 10 s), cleared after each write so idle streams have none; a stuck write fails, net/http drops the connection and cancels the request, and the sender exits. POSTs in flight are capped overall (64) and per principal (32). `calls.go`: tool calls in flight are capped per session (8) and per principal (32), counted as calls so dropped stateful POSTs still count, and refused before the upstream; a DELETE from the session's principal and `Proxy.Close` cancel calls (go-sdk's session Close only waits). Tests: `TestHTTPStuckAgent` (both eras: the stuck write had a deadline and timed out, the sender exits, another principal's call succeeds meanwhile, goroutines settle; it fails with the deadlines removed), `TestHTTPCallCaps`, `TestHTTPInFlightCap`, `TestHTTPSessionCap`. Review of PR #68 (T0.38): stateful sessions are also capped per principal (4 of 16, `MaxSessionsPerPrincipal`), so one principal cannot pin every slot; fathomgate runs its own idle expiry (`liveSession`: no POST in progress for `SessionTimeout`), which cancels the session's calls before closing it, because go-sdk's idle Close waits for a stuck call forever; every 401, 403, 404, 413 and 503 carries `Connection: close`, so refused clients hold none of the 128 connection slots; the write deadline is cleared at the start of each request. `cmd/fathomgate`'s server registers `Proxy.Close` on shutdown, so a 2025-era GET stream does not hold `Shutdown` to its deadline. Tests: `TestHTTPSessionsPerPrincipal`, `TestHTTPIdleExpiryCancelsStuckCall` (hangs without fathomgate's expiry), `TestHTTPIdleExpiryWaitsForOpenPOST`, `TestPOSTBeforeRegistration` (T0.45: a POST that reaches a new session before fathomgate registers it counts as in progress once it is registered, so the idle clock never runs under it), `TestHTTPRefusalsCloseConnection`, `TestHTTPKeepAliveDeadlines`, `TestShutdownWithStatefulGET` (`cmd/fathomgate`). Residual: a stateful call runs until it ends, its session is deleted or 30 minutes pass with no POST on the session, within the caps. T0.52 (L1 in the security review of PR #109): a connection that sent no request held a slot for `ReadHeaderTimeout` (10 s) without a token. `cmd/fathomgate/listen.go` `newHTTPServer` now starts a 3-second timer per connection in `http.Server.ConnContext` (`firstHeaderTimeout`) that closes it, and the handler stops it when the first request's headers are in (`firstRequest`), so a silent or trickling client is dropped then; later requests keep 10 s. Documented hooks only: no dependence on net/http's deadline order, and it works under TLS (S2 in the Go review of PR #112). The 128 slots are shared by both loopback listeners. `TestListenerEndToEnd` checks through the real server that 401 and 403 carry `Connection: close` on both addresses (Go review of PR #109, item 5). Tests: `TestFirstRequestTimer` (no clock), `TestFirstHeaderTimeoutSmoke`, `TestLimitListenersShareSlots`, `TestLimitedConnCloseWrite`, `TestListenerEndToEnd`. An idle listener's Accept holds one of the shared slots while it waits (`limitListeners` godoc). Residual, accepted (T0.52): a local user can still keep every slot busy by opening connections faster than they time out (about 43 a second, up from 13); the listener is loopback-only, so the attacker is a local user, whom M0 does not defend the proxy's availability against. T0.57: an agent that crashed or restarted without a DELETE left sessions that held its principal's slots for up to 30 minutes, so its new `initialize` got 503. When a new session would pass a cap, `reserveSession` now evicts the principal's own least recently used session that is idle (no POST in progress, no GET stream, no call in flight), answers 404 to its later requests and closes it on a tracked goroutine; another principal's sessions are never candidates, and with none idle the 503 stands and is logged with what kept each session in use (at most one line a second per principal, with a `suppressed` count). So the caps still bound the sessions in use. Least recently used is a sequence number, not a clock reading, so a coarse clock cannot tie two sessions. **Claim and refuse** (fix round of PR #121, found by both reviews): a `tools/call` go-sdk has delivered but that has not reached `admit` (its agent dropped the POST) left the session looking idle, so eviction could cancel it mid-flight or let it run on a closed session with no idle clock while the slot served another session. `callLimits.claimIdle` now checks for calls in flight and retires the session in one step under `callLimits.mu` (lock order `httpHandler.mu`, then `callLimits.mu`), and `admit` refuses any call on a retired session with a tool error before anything reaches the upstream. The idle expiry retires the session the same way as it cancels its calls (`callLimits.retire`), closing the same window there. Tests: `TestEvictionRefusesUnadmittedCall` and `TestIdleExpiryRefusesUnadmittedCall` hold the call between delivery and `admit` (`Proxy.testHookBeforeAdmit`). Residual, accepted (N3 in the security re-review of PR #121): a DELETE does not retire the session, so a call go-sdk delivered before the DELETE but that `admit` had not yet let through is admitted after the DELETE's `cancelSession` and runs. go-sdk's `serveStatefulDELETE` calls the session's `Close` synchronously, which waits for that call, so the session stays registered. A DELETE does not stop fathomgate's idle clock, so `expire` fires after `SessionTimeout` and `retire` cancels the call. The bound is therefore `SessionTimeout` (30 minutes), the same residual already accepted for a call whose POST was dropped. It is pinned by `TestDeleteWindowBoundedByIdleExpiry`. Retiring on DELETE is not done, because go-sdk can still refuse the DELETE (a 400 on a bad header) after fathomgate has retired a live session; T0.58 revisits it for M1. New and accepted by security-reviewer: a holder of a principal's token can end that principal's idle sessions by opening new ones, without their ids (before, it could only fill the slots and lock the other clients out). With a **shared token** this reaches other clients: a hostile holder can keep evicting the other clients on that token, and each one's new session then has its stateful upstream's prompts refused for up to `OrphanTTL` (the orphan rule, row 41). Mitigation: one token per agent (profile-schema 8.5, docs/install.md). Sessions with a GET stream open stay non-evictable (security-reviewer, 2026-09-25). The eviction's Info line is not rate-limited; each eviction needs an `initialize`, so the POST caps (64, 32 per principal) bound it. Tests: `TestHTTPSessionCapEviction`, `TestEvictedSessionRefusedBeforeSDK`, `TestGETBeforeRegistration`, `TestCapLogPerPrincipal` |
| DNS rebinding and cross-site requests to the loopback listener: a web page in a local browser drives the policy-free proxy | MCP07 (auth) | Mitigated (T0.27) | `http.go`: a non-loopback `Host` on a loopback bind gets 403 before authentication (go-sdk's check stays on too); any `Origin` header, including `null` and empty, and any `Sec-Fetch-Site` but `none` or `same-origin` get 403 on every method before authentication; no CORS headers; OPTIONS gets 405. The standard library's `CrossOriginProtection` is not used (it exempts GET, HEAD and OPTIONS and accepts `Origin` equal to `Host`). Tests: `TestHTTPChecks`. Browser-hosted clients cannot connect (accepted, ADR 0016) |
| Listener token guessing, timing or leakage | MCP07 (auth) | Mitigated (T0.27) | `http.go`: tokens at least 32 bytes of printable ASCII, validated without quoting them; only SHA-256 digests kept; the presented token's digest compared with every configured digest in constant time without an early exit; 401 names no token; refusals logged at most once per second with remote address and reason, never the header. Several named tokens (no unnamed ones), each a principal for attribution only, never an approver (invariant 6); tokens must be random (`openssl rand -hex 32`), which fathomgate cannot check; go-sdk binds a stateful session to its principal (403 for another's requests, and another's DELETE cancels nothing). Tests: `TestHTTPChecks` (missing, wrong, truncated, extended, Basic), `TestHTTPOptions`, `TestHTTPLogs`, `TestHTTPSessionBinding`. Token sources and file checks: next row (T0.31) |
| Session id exposure: a logged `Mcp-Session-Id` lets anyone who reads the log act on the session | MCP07 (auth) | Mitigated (T0.27) | `http.go` logs session ids only as the first 12 hex digits of their SHA-256 (`shortHash`); go-sdk's id generator (`crypto/rand.Text`) is not overridden, and a session also needs its principal's token. Test: `TestHTTPLogs` |
| Listen token exposed on the host: typed on the command line (`ps`, `/proc/<pid>/cmdline`, process auditing, the MCP host's config file), kept in a file another local user can read or replace (group or world bits, an inherited Windows ACL, a symbolic link or hard link to a file someone else controls), or echoed in fathomgate's output; on the client side (T0.33), an exported or persisted variable holding it, inherited by every process the MCP client starts, a project `.mcp.json` naming that variable, or a literal token in the client's config | MCP07 (auth) | Mitigated (T0.31); client side open, documentation only (T0.33) | `cmd/fathomgate`: no flag takes a token value (`--listen-token` is an unknown flag, reported by position); tokens come from `--listen-token-file NAME=PATH` or `FATHOMGATE_LISTEN_TOKEN`, not both. `internal/secretfile` (`secretfile_unix.go`; shared with the audit signing key since M1-09, ADR 0028) opens with `O_NOFOLLOW` and `O_NONBLOCK` and checks the descriptor: regular file, one link, owned by the effective user, no group or other bits. `secretfile_windows.go` opens with `FILE_FLAG_OPEN_REPARSE_POINT` and checks the handle: disk file, not a reparse point or directory, one link, owned by the current user, protected DACL whose allow entries name only that user and `SYSTEM`. Other platforms refuse token files. No error quotes a token, a path or the argument; tokens are scrubbed from stderr as `[redacted:listen-token:<name>]` and never passed to the upstream (`FATHOMGATE_*` never crosses, ADR 0017). Tests: `TestReadTokenFileUnix`, `TestReadTokenFileWindows`, `TestLoadListenTokens`, `TestServeListenRefusals` (every refusal exits 2 with no token, path or refused value in stderr), `TestServeListenProcess` (no token in stderr after a real run). T0.52 (L2 in the security review of PR #109): the mode check did not see a macOS extended ACL (`chmod +a "everyone allow read"`, or entries inherited from the folder). `internal/fileacl` reads `ATTR_CMN_EXTENDED_SECURITY` with `fgetattrlist(2)` on the open descriptor, through `syscall.Syscall6`, which on darwin is a raw trap (`SVC $0x80` on arm64, `SYSCALL` on amd64 in Go 1.26's `syscall/asm_darwin_*.s`), not libc (no cgo, no `x/sys/unix`); `readTokenFile` refuses a file with any ACL entry, or whose ACL cannot be read. `internal/audit` had the same gap for the key and the log and is fixed the same way: `createExclusive` deletes and refuses an owner-only file that came out with an inherited ACL, and `checkLogFile` refuses an existing log with one. Tests (run on `macos-latest` in CI with real ACLs): `TestExtendedDarwin`, `TestReadTokenFileDarwinACL`, `TestDarwinInheritedACLRefused`, `TestDarwinExistingLogWithACLRefused`; `TestParseAttrBuf` on every platform. Residual, accepted: FreeBSD and illumos NFSv4 ACLs are not read (Linux POSIX ACLs show in the group bits, which are checked). SMB and NFS mounts on macOS report no ACL whatever the server enforces, so a token file or key there is checked by its mode alone; keep them on a local disk. Apple does not promise the system-call ABI: an error return refuses the file, a number the kernel no longer has raises SIGSYS, which the Go runtime treats as fatal on darwin (fathomgate crashes; nothing is accepted), and a reused number that returns success without filling the buffer leaves a length `parseAttrBuf` refuses (`TestParseAttrBuf`, "buffer never written"), so every path fails closed. CI's `macos-latest` runner is arm64: the darwin/amd64 build is linted, not run. Residual, accepted: the environment variable is readable by the same user or root (`/proc/<pid>/environ`), like `--upstream-env-pass` values; the proxy host is trusted. An administrator can still read an owner-only file. N1 (T0.52): any process running as the same user can read the token file or fathomgate's environment (`/proc/<ppid>/environ`) and then act as that principal, and that includes the upstream, which runs as fathomgate's child with the same user: an upstream that is hostile or compromised can call its own tools, and any other upstream's, through the listener under an operator's principal. Accepted for M0 (the upstream is already trusted with the device credentials); run fathomgate as a user of its own to separate it from other software, and from M1 the policy applies to such calls like any other Client side (T0.33, security review of PR #115, M1 and L1), open, mitigated by documentation only: fathomgate cannot see how a client stores or passes the token. (1) A variable exported in a shell, a shell rc file, a PowerShell profile or `setx` reaches every program started from there, and every process Claude Code starts inherits it: its Bash tool, hooks and stdio MCP servers. (2) Claude Code expands `${VAR}` in any HTTP server's `headers` and `url`, so an approved project `.mcp.json` that names the variable sends the token to whatever URL that file gives. (3) A token pasted into `claude mcp add` lands in shell history and `~/.claude.json` (or `.mcp.json` with `--scope project`), and `claude mcp get` prints it; with a `${VAR}` reference, `claude mcp get` prints the reference, not the value (checked with Claude Code 2.1.281). [install.md](../install.md#remote-agents-over-http) therefore sets the variable for the `claude` process alone (`CLAUDE_FATHOMGATE_TOKEN="$(cat <file>)" claude`, or a PowerShell window used only to start it), forbids rc files, profiles and `setx`, keeps the token out of `--scope project`, tells users to read a project's `url` and `headers` before approving it, and uses a per-client token file and principal (`claude-code`) so one client's token can be withdrawn (entry removed or file replaced, then a restart) without the others |
| Listener answering for a dead upstream, or held open at shutdown | MCP05 (availability) | Mitigated (T0.31) | `cmd/fathomgate/listen.go` `runListener`: `Proxy.UpstreamExited` stops the listener and exits 1, so a supervisor restarts fathomgate instead of it answering `upstream is not running` indefinitely; SIGINT and SIGTERM exit 0. Shutdown gives requests in flight (every method but GET) 5 seconds, then `Proxy.Close` cancels the rest and closes the sessions, so GET streams end; connections still open 5 seconds later are closed by force. The address is bound before the upstream starts. Tests: `TestListenerEndToEnd`, `TestListenerUpstreamExit`, `TestServeListenProcess` (real upstream process exits mid-serve), `TestShutdownGrace`, `TestShutdownWithStatefulGET`, `TestUpstreamExited`, `TestServeListenBindFails`; `cmd/fathomgate` now runs the stdlib goroutine-leak check after its tests. T0.52: a 2026-era `subscriptions/listen` stream no longer counts toward the grace (Go review of PR #109, item 1; `longLived`, `TestShutdownWithListenStream`, `TestLongLived`); today go-sdk answers one on fathomgate at once, since the proxy declares no `listChanged`, so this is for when it does. N2 (security review of PR #109): shutdown has no overall deadline (up to 10 s for the grace and the connections, then the upstream's own stop), and a second SIGINT or SIGTERM was ignored meanwhile; `serve` now stops catching signals once the first arrives (`context.AfterFunc(ctx, stop)`), so a second one ends fathomgate at once. Residual, accepted: that second signal cleans nothing up, so the upstream can outlive fathomgate (row on the orphaned grandchild) |
| go-sdk compatibility switches through the environment (`MCPGODEBUG=allowsessionsinstateless=1` and others change transport security without appearing on the command line) | MCP07 | Mitigated (T0.31), residual accepted | `fathomgate serve --listen` refuses a set `MCPGODEBUG`, even empty, with exit 2 before it binds or spawns (`checkListenEnvironment`, S6 in the security review of T0.40), and `HTTPHandler` refuses it again. Tests: `TestServeListenRefusals` (set and empty), `TestCheckListenEnvironment`. Residual: a value go-sdk cannot parse makes go-sdk's package initialisation panic before fathomgate runs, so every command exits 2 with a stack trace; that fails closed. The stdio side does not refuse `MCPGODEBUG` (ADR 0016 scopes the refusal to the listener) |
| Stateful upstream prompt misattributed across agent sessions: a call ends on fathomgate's side (the agent cancels, a 2026-era POST drops, a write deadline or DELETE ends it) while the upstream, ignoring `notifications/cancelled`, keeps working and later sends `elicitation/create` for it; another agent session's call is by then the only one in flight, so that session's human is shown the first session's prompt and their answer goes back as its answer (confused deputy) | MCP03 (confused deputy) | Mitigated (T0.38, H1 in the review of PR #68; widened by T0.40, J1) | `internal/proxy/input.go`: a call that ends is recorded, in the same critical section that removes it from the in-flight set, as an orphan of its agent session for `OrphanTTL` (5 minutes); `attribute` relays a prompt only when there is exactly one call in flight and every live orphan on the upstream belongs to that call's session, and otherwise refuses it (JSON-RPC error to the upstream; a note naming no session, principal or prompt text on the results of the calls in flight). Orphans are keyed by fathomgate's own key for the agent session, never by the session itself, and the key comes from what the session is: the local agent `Proxy.Run` serves has a key of its own whether or not an HTTP listener exists, so building the listener does not key it as a per-request call (T0.43, S1 in the post-merge security review of T0.40), and two agents on one proxy never share a key. A call fathomgate cannot name to a session (a stateless per-request session, a call with no session, a local call whose context ended before `Run` recorded its session) gets a key of its own that no other call gets, so its orphan is an ordinary record, foreign to every later call (T0.44). At most 256 keyed records per principal per upstream; past that, one overflow record of that principal's blocks every session, its own included. When the orphan rule refuses a prompt, the refusal is never put in place of the upstream's error, including when it is worded from the one call in flight (T0.42, S3): a hostile upstream could otherwise have made fathomgate state a reason it invented for a call's failure. Tests: `TestHTTPAbandonedCallPrompt` (the proof of concept, with the abandoning agent stateful and stateless; before the fix bob's accept reached the upstream as the answer to alice's prompt), `TestHTTPAbandonedCallPromptSameSession`, `TestOrphanAttribution`, `TestAgentSessionKey`, `TestLocalAgentOwnOrphanWithListener`, `TestLocalKeyDuringConnect` (a local call dispatched while `Run` is still connecting waits for the record and is keyed as that session's, never as a per-request call), `TestListenerCallNeverWaitsForRun` (a listener call never waits for a connecting Run, so it cannot sit outside the call caps), `TestOrphanRefusalNeverReplacesUpstreamError`. Residual, accepted: within one agent session an orphan's prompt can reach that session's later call (same human); the refusal note tells the agent that an upstream prompt existed on that upstream around the time of its call (existence and timing), never its text or whose it may have been; and which refusal an agent sees (the orphan reason, or for a stateless agent whether the ADR 0014 wording arrives as a note or as fathomgate's own error) reveals whether another agent session ended a call on that upstream within `OrphanTTL`, the same disclosure the `errEndedElsewhere` text already makes. The cross-principal effect of a principal that keeps ending calls is its own row below (T0.44). T0.57 kept the rule for an agent that restarts: its new session, same principal, does not inherit the ended calls of its old one, so its stateful upstream's prompts are refused for up to `OrphanTTL` (5 minutes) after the restart. Inheriting by principal would let a prompt for a call the old conversation abandoned reach a human who never saw it, as the answer to that call (a principal is a token, not a conversation); the cost is a bounded, logged refusal (`ended_calls_of`) with no prompt shown. Pinned by `TestOrphanAttribution` (both sessions share one principal) and, through the listener, by `TestEvictedSessionOrphanStillBlocks` (L1 in the security review of PR #121): session S1 ends a call on a stateful upstream and is evicted by its principal's new `initialize`, S2 makes a call, and the upstream's `elicitation/create` is refused with `errEndedElsewhere` until `OrphanTTL`, with no prompt shown. Spec: profile-schema section 8.4, *Attribution across agent sessions* |
| Stateful upstream prompt for a call that ended **normally**: the upstream answered the call and kept working (a background job), or sent `elicitation/create` just before its result and go-sdk dispatched the result first; fathomgate tracked only cancelled calls, so the finished call left nothing behind and another agent session's human was shown its prompt, their answer going back as its answer | MCP03 (confused deputy) | Mitigated (T0.40, J1 in the re-review of PR #72) | `internal/proxy/proxy.go` records **every** call that ends as an orphan of its agent session, not only a cancelled one: fathomgate cannot tell whether an upstream is still working on a call it answered. `attribute` then refuses the prompt as above. Test: `TestHTTPPromptAfterFinishedCall` (alice's call returns "late done", the upstream prompts afterwards while bob's call is the only one in flight; it fails on the pre-T0.40 rule). Residual, accepted (keyed sessions since T0.40; per-request keys and the overflow record by the maintainer, 2026-09-24, next row): with more than one agent session on one upstream, a stateful upstream's prompt is refused after any other session's call ends, and relayed prompts become rarer as agents share an upstream. `OrphanTTL` (5 minutes) bounds the effect of **one** ended call, not the steady state: each ended call renews its session's orphan, so an agent that keeps calling keeps other sessions' prompts refused for as long as it keeps calling. An agent need not end anything either: a principal that holds one long call open on the upstream refuses every other session's prompts on it for as long as the call runs, because a prompt is never attributed with two calls in flight. Every orphan, per-request records and overflow records included (T0.44), is logged at Info with the server and the principal it was refusing prompts for when it is reaped. Every refusal of an unattributed prompt is logged at Warn with the principals behind it, `ended_calls_of` for the orphan rule and `in_flight_of` for the calls in flight; an attributed one names its call's `principal`. Every refusal of an upstream input request, attributed to one call or not, is logged at Warn at most once per 10 seconds per server, attribution, class of refusal and set of principals, with a `suppressed` count of the lines held back since the last (and `suppressed_lost` when the limiter swept its table and dropped held-back counts of other keys), so a looping upstream cannot flood the log. The exception in 8.4 keeps the ADR 0014 wording when the one call in flight could not have been prompted anyway, as a note on that call (T0.42). The same effect from stateless requests and past the per-principal quota is the next row. Spec: profile-schema sections 8.4 and 8.5, *Orphan TTL* |
| Cross-principal refusal of stateful prompts via the shared orphan entry: every stateless per-request session, and every stateful session past 1024 per upstream, was recorded in one shared entry that was foreign to every session. One authenticated principal could keep every other principal's stateful upstream prompts on an upstream refused with one 2026-era request per `OrphanTTL`, or by ending calls on more than 1024 sequential stateful sessions, and the entry was not pruned, not logged and carried no principal, so such a refusal could not be traced to its cause from the log (S2 and S4 in the post-merge security review of T0.40) | MCP05 (availability) | Accepted (residual), maintainer, 2026-09-24. S4 and the global-overflow amplification are mitigated in T0.44 | The shared entry is gone. Every call fathomgate cannot name to a session gets a key of its own (`r` and a per-proxy counter, which can never collide with a stateful session's `s<id>` or a local agent's `l<n>`; a stateless agent's session is never keyed by an id). Its ended call is an ordinary orphan: foreign to every later call, pruned at `OrphanTTL`, logged at Info with its principal when reaped (maintainer decision 2026-09-24). Each refusal of an unattributed prompt gets one Warn line naming the principals behind it: `ended_calls_of` when the orphan rule refused it, `in_flight_of` when not exactly one call was in flight or when the one call's agent could not be prompted (ADR 0014). A refusal attributed to one call (an agent without form elicitation, a busy prompt slot, too many prompts) names that call's `principal`. Every refusal of an upstream input request, attributed to one call or not, is logged at Warn at most once per 10 seconds per server, attribution, class of refusal and set of principals, with a `suppressed` count of the lines held back since the last (and `suppressed_lost` when the limiter swept its table and dropped held-back counts of other keys), so a looping upstream cannot flood the log. The text the agent and the upstream see names no principal. The 1024-entry table cap is replaced by a quota of 256 keyed records per principal per upstream (accepted by the maintainer, 2026-09-24). Past it, that principal's further ended calls fold into one overflow record of its own. The record is foreign to every call, that principal's own sessions included (fails closed). It is logged at Warn once when it is made and at Info when it is reaped, with the number of ended calls it stood for, and it cannot push another principal's records out. It refuses other principals' prompts exactly as long as the ended calls it stands for would have as keyed records, so it gives a principal no reach over others that its ordinary ended calls lack. The table holds at most 257 records per principal per upstream, and principals are configured. The quota Warn is expected under normal load: each 2026-era request leaves its own record, so a stateless agent making more than about 0.85 calls a second to one upstream (256 per 5-minute `OrphanTTL`) crosses it. The maintainer kept one record per stateless request (2026-09-24). Tests: `TestHTTPStatelessOrphanPerRequest` (alice's stateless requests leave per-request records; bob's prompt is refused only while they are live and the log names alice; alice's own ADR 0014 refusal names no ended calls; after the TTL the records are reaped, logged with alice, and bob's prompt is relayed), `TestOrphanQuotaPerPrincipal`, `TestOrphanQuotaLogged`, `TestOrphanModelNeverFailsOpen` (the security reviewer's randomised reference model, strengthened in the re-review of PR #82: floods, quiet phases and a probe whenever nothing is in flight make every seed reach relays and probes under a live overflow record; it fails both when `attribute` ignores the overflow records (fail open) and when it treats a session's own record as foreign (over-refusal)), `TestAttributedRefusalRateLimited`, `TestUnattributedRefusalLog`, `TestRefusalLimiterBounded`, `TestStatelessSessionHasNoID`, `TestOrphanAttribution`, `TestAgentSessionKey`. Residual, accepted by the maintainer (2026-09-24): an upstream's `elicitation/create` names no call, so any principal that can call a stateful upstream can keep every other agent session's prompts on it refused, by ending one call per `OrphanTTL` or by holding one call open. It fails closed (no prompt reaches the wrong human, nothing leaks) and the log names the principal. The only recourse is to remove that principal's token (a restart in M0; there is no live revocation). Shortening `OrphanTTL` is not a recourse: it gives up the confused-deputy protection of the rows above, since another session's slow prompt would then reach the wrong human. From M3 a refused prompt may be an approval or a credential request: the row below. Spec: profile-schema sections 8.4 and 8.5 |
| Prompt availability from M3: with the residual above, one principal that can call a stateful upstream can keep an approval or a credential request that upstream puts to another principal's human refused, by ending a call per `OrphanTTL` or holding one open | MCP05 (availability) | Open. Owner: mcp-protocol-engineer; M3 | The attribution rule cannot tell whose prompt it is, so in M0 it fails closed and logs the principal (row above). Before approvals ride on upstream prompts (M3), fathomgate needs either a per-upstream prompt policy (which upstreams may prompt at all, and whose agents may receive them) or to bind a prompting stateful upstream to one principal, so another principal's calls cannot make its prompts ambiguous. Decided by the maintainer, 2026-09-24. Spec: profile-schema section 8.4 |
| Relayed prompt sent on the wrong stream: a stateful upstream's `elicitation/create` sent under the upstream request's context reaches no HTTP stream (or, in a future go-sdk, another request's) | MCP03 | Mitigated (T0.27) | `input.go` sends the relayed prompt under the attributed agent call's context, cancelled by either side. Test: `TestHTTPEraMatrix` (2025 agent, 2025 upstream) |
| Prompt schema round-trip: a value that crosses a prompt verbatim (property name, `enum` or `const` value, default) carries text a reader decodes differently from what the upstream gets back, or a default reaches the human altered and is sent back altered | MCP03, MCP05 | Mitigated | `internal/proxy/schema.go`: such values cannot be escaped without changing what the upstream receives, so property names and `enum`/`const` values that `escapeControl` would change (a control character or backslash) are refused, and such defaults are dropped (the human types the value). Tests: `TestRelabelSchema` ("default with a backslash dropped", "array default with a backslash dropped", "property name with a backslash", "enum value with a backslash") |
| Unsolicited `inputResponses`: an agent pre-answers an upstream prompt no human was shown, or a later stage mistakes agent-supplied answers for a human's | MCP03 (confused deputy) | Mitigated | `internal/proxy/proxy.go`: `newCall` clears answers sent without fathomgate's `requestState` before `Proxy.dispatch` (the M1 seam), and `forward` sends a first call; `resume` drops answers to ids that are not outstanding. Invariant 6. Tests: `TestNewCallClearsUnsolicited`, `TestUnsolicitedInputResponses`, `TestRetryIgnoresUnknownIDs`; strict cases in `TestMRTRWire`. Conformance `ignore-extra-params` stays baselined for this reason |
| Upstream secret on the command line: a device password in fathomgate's argv is visible to other local users (`ps`, `/proc/<pid>/cmdline`) and recorded by process auditing (`auditd`, event 4688, Sysmon, EDR) | — (credential exposure) | Mitigated | `--upstream-env-pass NAME` reads the value from fathomgate's environment (ADR 0017); `cmd/fathomgate/serve.go`. Tier 2 `test_password_is_never_on_the_command_line` |
| Upstream secret in the environment: `/proc/<pid>/environ`, `ps -E`, crash dumps of fathomgate or the upstream | — | Accepted (out of scope) | Readable by the same user or root only; the proxy host is trusted (SECURITY.md). The upstream needs the value in its environment |
| Upstream secret echoed by fathomgate's own errors: a password typed as its own argument (`--upstream-env DEVICE_PASSWORD FAKE-x`), a dash-prefixed one read as an unknown flag, a malformed `--upstream-env` argument or key | — | Mitigated (PR #64, E1 and E2) | `serve.go`: stray positionals, unknown flags, bad syntax and malformed `--upstream-env` arguments are reported by position only; the flag package's own messages are discarded. Test: `TestServeErrorsNeverCarryValues` (canary in every path) |
| Valid-looking name echo: a value typed where a name belongs (`--upstream-env-pass Summer2024`) is printed in the "not set" error | — | Accepted | ADR 0017 requires the error to name the variable; the separate flag (alternative (e) rejected) keeps the case rare |
| Upstream prints the secret on stderr | — | Mitigated, residual accepted | `internal/proxy/scrub.go`: the value and its Go-quoted, JSON, percent-encoded, Python byte-escaped and backslash-doubled forms become `[redacted:NAME]` on the raw stream before splitting and escaping; everything `serve` writes to stderr is redacted again (`redactingWriter`). Residual: values under 4 bytes, base64, HTML entities, a Python repr that escapes a quote, other transforms, the partial leak where two passed values overlap and the longer one wins, and upstream text that fathomgate cuts before `redactingWriter` sees it: the hybrid refusal (`hybridError`, M1-32) quotes the upstream's answered protocol version clipped at 200 bytes, so a passed value that straddles the cut reaches stderr as a prefix the scrubber no longer matches in full; the clip is followed by `...` and the rest of the error, so it is never a stream tail. Accepted: the answered version is a field no real server fills with anything but a date, and the leak needs an upstream that already holds the passed value. Tests: `scrub_test.go`, `TestServeKeepsPassedValuesOffStderr` |
| Scrubber tail released early: stderr that arrives after the line writer's Flush (go-sdk's `pipeRWC.Close` can return before `Wait`) completes a value whose first bytes were released | — | Mitigated (PR #64, E3) | `lineWriter.Flush` is terminal; later writes are dropped. Test: `TestLineWriterFlushIsTerminal` |
| Secret formatted into a log or error through a struct | — | Mitigated (PR #64, E4) | `proxy.Secret` is opaque: `Format`, `String`, `GoString`, `MarshalJSON`, `LogValue` return the marker; the value sits behind a pointer, so reflection into an unexported field prints an address; values join the child environment only at exec time, not `Command.Env`. Test: `TestSecretNeverFormatsItsValue` |
| Orphaned grandchild after the ADR 0018 restart (proxy ↔ upstream): `--upstream` is a launcher (`uvx`, `npx`, `uv run`, a shell script, `docker run -i`), fathomgate kills only that direct child when the probe goes unanswered or startup fails, and the real server underneath keeps running, still holding the `--upstream-env-pass` credentials in its environment and possibly a device session, next to the second copy fathomgate starts | — (credential exposure, availability) | Mitigated (T0.46, [ADR 0021](../adr/0021-kill-the-upstream-process-tree.md); S2 in the review of PR #77), residuals accepted | `internal/proxy/tree_unix.go`, `tree_windows.go`, `tree.go`; `Command.Transport` (`prepareTree`), `trackedTransport.Connect` (`attachTree`), `killProcess`, `trackedConn.Close` (`mirrorShutdown`, `sweep`). The upstream starts in a process group of its own on Unix (`Setpgid`, set between fork and exec, so there is no window) and in a Job Object of its own on Windows (`KILL_ON_JOB_CLOSE`, no breakaway; created suspended, assigned, then resumed through a Toolhelp32 thread snapshot, failing closed). The restart, every failed attempt and the startup budget kill the whole tree; shutdown signals the group 250 ms after go-sdk signals the upstream; after the reap the tree is swept once (SIGTERM, 2 s, SIGKILL; the job is terminated on Windows). The startup overrun of up to 2 s (`WaitDelay`, L1 in the re-review of PR #79) is gone on the kill paths. Tests (Linux, macOS and Windows CI, on a launcher fixture whose child ignores stdin EOF and SIGTERM): `TestTreeRestartKillsGrandchild`, `TestTreeCloseSweepsAfterLauncherExits`, `TestTreeStartupBudgetNoWaitDelay`, `TestTreeExitWatcherSweeps`, `TestTreeUnixGrandchildInGroup`, `TestTreeWindowsGrandchildInJob` (`IsProcessInJob`); after the PR #111 reviews also `TestTreeAttachFailsClosed` (an injected attach failure: ADR 0021 error, killed and reaped, never ran on Windows), `TestTreeWindowsStartsSuspended` (suspend count at least 1 and no marker before `attachTree`; fails without `CREATE_SUSPENDED`), `TestResumeProcessExited` and `TestProcTreeSweepAfterKill`. Residuals, accepted in ADR 0021 and its 2026-09-25 amendment: the group and the job are containment for well-behaved launchers, not a sandbox. On Unix a descendant that calls `setsid()` or `setpgid()` leaves the group. On Windows any descendant can start a process outside the job by naming a same-user process outside the job, fathomgate included, as its parent (`PROC_THREAD_ATTRIBUTE_PARENT_PROCESS`, one unprivileged call), or through a service (WMI, the Task Scheduler); and one that can open fathomgate with `PROCESS_DUP_HANDLE` can duplicate the job handle, so kill-on-close no longer fires when fathomgate dies (security review of PR #111, L1, verified). An escaped process is bounded on stderr by `WaitDelay`. A setuid launcher (`sudo`) puts a member in the group that fathomgate may not signal: every group signal (kill, shutdown, sweep) gets `EPERM`, the first one is logged at Warn (`the upstream's process group has a member fathomgate may not signal (EPERM)`), and that member may outlive the upstream (N2). `docker run -i` is not covered, because the container belongs to the container runtime, not to the `docker` CLI. A fathomgate crash on Unix orphans the tree (systemd's `KillMode=control-group` covers it). The post-reap sweep is PID-based: exact while any group member lives, and during its poll window (up to 2 s, every 20 ms, `kill(-pgid, 0)`) a group that has emptied could in principle be matched by a reused PID that became a group leader (N1). With fathomgate as PID 1 and no init (the distroless image run without `docker run --init`), exited members stay zombies that keep the group non-empty: a sweep after SIGKILL no longer waits (L3), but a sweep after the leader exited on its own waits its full 2 s; install.md tells operators to use `--init`. An upstream that opens the controlling terminal of an interactive shell is stopped (SIGTTIN). Upstreams opening fathomgate's own process on Windows (N5) is tracked in T0.53. A tier 2 case (`uv run` in front of upa/mcp-netmiko-server on its own `uv.lock`) is for the test-engineer; until it and a manual check on each OS pass, install.md keeps advising `--upstream` at the server. The listener's shutdown and the exit-1 restart loop (T0.31: the upstream exits, fathomgate exits 1, a supervisor starts a new one) go through the same shutdown path, so each round kills the whole tree before the next starts. A second SIGINT or SIGTERM during shutdown (T0.52, N2) ends fathomgate at once: on Windows the job's kill-on-close then kills the tree, as it does whenever fathomgate's process ends; on Unix nothing runs after that signal, so the group is orphaned as in a crash (systemd's `KillMode=control-group` covers it, and install.md says the upstream may be left running) |
| Upstream echoes the secret back toward the agent | — | Partly mitigated | JSON-RPC error messages relayed to the agent are redacted before escaping and the 512-byte cap (`relayUpstreamError`, `callFailed`; PR #64, E5; `TestRelayUpstreamErrorRedacts`). Open: tool results, progress messages and elicitation text reach the agent unscrubbed until the M2 response-serialiser redaction (invariant 4) |
| Upstream logs device commands and output to stderr: upa/mcp-netmiko-server writes each command, each full config command list, and the first 100 characters of device output to stderr at INFO (`main.py:187, 202, 234` at `96e8ff3`). fathomgate relays upstream stderr into its own log, so a config line holding a secret (`username x secret y`) or the start of a `show running-config` lands in the operator's log | — (credential exposure) | Open (security review of PR #150, finding 3). Owner: M2 (redact relayed upstream stderr with the response redaction path) | `internal/proxy/command.go` (`Stderr`, `StderrPrefix`) relays the lines; `internal/proxy/scrub.go` replaces only `--upstream-env-pass` values, not device secrets. Operator warning in `profiles/upa.yaml` and research brief 02 §1.3a: treat fathomgate's log as holding device configuration until M2 |
| Target alias drift (upa/mcp-netmiko-server): the upstream's TOML inventory maps each device name to a `hostname` and is re-read on every call (`main.py:106-131, 190, 222`). fathomgate classifies and resolves the name, never the address the upstream connects to, so editing the TOML can point `lab-leaf-01` at a core router and a write reaches it under the lab rule instead of the prod hold | MCP07 (authorisation) | Open (security review of PR #150, finding 4). Owner: M1, moved from M2 (security review of PR #154): live in M1, because `lab-open`'s `lab-writes-free` forwards a lab write with no dry run or diff (ADR 0026 decision 1), so a drifted alias is written to as the agent sent it. Option: cross-check `get_network_device_list` hostnames against the inventory | Mitigations today, in `profiles/upa.yaml` and research brief 02 §1.3a: the TOML's device names must match fathomgate's inventory one-to-one, and the TOML must not be writable by the agent or any tool the agent can call |
| Pattern-resolved target (reads and writes): the target name is chosen by the agent, and before ADR 0031 `internal/inventory/patterns.go` `Resolve` made any name matching a hostname pattern a known device (`known: true`), with the pattern's role and tags, so the unknown-target default no longer applied. Patterns are case-insensitive and not end-anchored unless written with `$`, and names containing `.`, `@` or `:` still match. **Reads:** with the previous example patterns (`^core-\|^border-`, `^lab-`, `^fw-`), `core-x.attacker.example`, `lab-x.attacker.example` and `fw-evil.example` were `allow` `reads-anywhere` for `READ_CONFIG` under all three example policies. A free-form-host upstream (netdev-ssh-mcp `get_config`) then logs in to a host outside the inventory, possibly the agent's own, with the operator's `DEVICE_PASSWORD` or SSH agent, exposing the credential, and returns config output that is unredacted until M2. **Writes:** with `^lab-` adding `tags: [lab]`, `lab-ghost-99`, `LAB-core-rtr-01`, `lab-x@core-rtr-01` and `lab-leaf-01.evil` were known lab devices, and `lab-open` allowed a write to each by `lab-writes-free`, with no dry run in M1. inventory-schema section 4 originally said a pattern match alone never makes a target known; the code did | MCP07 (authorisation) | Mitigated (M1-34, [ADR 0031](../adr/0031-hostname-patterns-never-make-a-target-known.md)): a hostname pattern is an enricher, not a provider that can hit. `inventory.Chain` makes a name known only when a name authority (the static file or CSV import in M1) lists it, and runs the patterns only on the listed name, filling a role or site the record leaves empty and adding tags; `Patterns` has no `Resolve`, so it cannot be put in the chain as an authority. `internal/gate` takes the resolver's answer (its interim `status: pattern` check is gone), and `policy eval` and `inventory resolve` apply the same exact-name rule. A pattern that matches no listed device is a warning at `serve` start and an error in `fathomgate inventory lint`. Residual, accepted (ADR 0031 decision 2 of its open questions): a role or tag a pattern gives a listed device counts for write rules, so a loose pattern can mislabel a device the operator listed (`^lab-` tagging a listed `lab-core-01`); that is ADR 0007's accepted misclassification, not an agent-chosen name. Residual, closed (security review of PR #184, L1 and R2-L1): a later name authority (from M2, possibly an upstream's own listing, untrusted data) runs after the patterns and only fills an empty site or status; it never sets role or adds tags, which unlock writes, and a record whose name is not the name looked up is ignored (inventory-schema section 2; `TestChainMergesAuthorities`, `TestLaterAuthorityNeverRaisesStanding`). Residual, open (M2 upstream-provider ADR): whether an upstream's own inventory listing is a name authority for writes at all. Hardening from the same review: `inventory.Known` is the one exact-name rule (gate, `policy eval`, `inventory resolve`); `policy eval` refuses a `--target` the gate refuses; `inventory lint` and `inventory resolve` read the file through `internal/configfile` as `serve` does, lint fails on a listed name the gate would refuse and warns for every role, site or tag a pattern adds to a listed device; `resolve` prints non-ASCII and control characters quoted; `status: pattern` is refused at load; the example's commented patterns set only a site or a tag no example rule matches | `internal/inventory`: `TestPatternsAloneResolveNothing` and `TestPatternsWithListedDevices` (the eight attacker names of ADR 0031 test 1 are unknown with `^core-\|^border-`, `^lab-`, `^fw-` active, with and without the example's devices), `TestRepoExampleInventory` (once as shipped, once with the example's commented pattern block turned on), `TestEnrichment`, `TestChainMergesAuthorities` (a record for another name ignored; a later authority's tags not added; empty-field fill), `TestKnown` (the Kelvin sign `\u212avm-01`, Cyrillic `lab-\u0455w-09`, zero-width space, trailing dot and leading space are unknown although the static file folds case), `TestPatternEffects`, `TestStatusNeverPattern`, `TestDeviceFileRefusesProvenance`, and the fuzz target `FuzzPatternsNeverResolveUnlisted` (any unlisted name is unknown whatever patterns are loaded, and `Known` is true exactly for a name listed byte for byte). `internal/gate`: `TestAttackerHostNames` (attacker names and the PR #184 review's probe names with the patterns active, from arguments to `default:unknown_target` or `default:bad_arguments` under read-only and lab-open) and `TestPatternEnrichesListedDevice` (a listed untagged `lab-sw-09` tagged by `^lab-` is `allow` `lab-writes-free`; `lab-ghost-99` and `core-x.attacker.example` are `deny` `default:unknown_target` for writes and reads). `cmd/fathomgate`: `TestEvalPatternsEnrichOnly` (ADR 0031 tests 7 and 8, and the probe names as `--target`, `default:bad_arguments`), `TestInventoryResolve`, `TestInventoryLint`. `policies/examples/lab-open.test.yaml`: the former "pattern hazard" case is now "a known device tagged lab is allowed to write" Gate cases (ADR 0035, M1-33): the PR #154 names under active patterns, from the raw tool call to the decision, in `policies/examples/read-only.gate.test.yaml` and `lab-open.gate.test.yaml`. |
| Upstream transport hazards accepted (upa/mcp-netmiko-server): no SSH host-key checking (`connect_kwargs`, `main.py:71-80`, sets neither `ssh_strict` nor `system_host_keys`, so netmiko uses paramiko `AutoAddPolicy`); `--sse` serves with no authentication; `--debug` turns on Starlette debug tracebacks (`main.py:248-282`) | MCP07 (auth) | Accepted (security review of PR #150, finding 5), with scope: fathomgate supports upa only over stdio as `fathomgate serve --upstream`; operators must not run it with `--sse` or `--debug` | Operator warning in `profiles/upa.yaml` and research brief 02 §1.3a. Host keys are the operator's to manage on the upstream host; fathomgate cannot see the SSH session |
| Upstream transport hazards accepted (eos-mcp, upstream ↔ device): eos-mcp 1.3.0 never verifies the eAPI device's TLS certificate. It passes `config.ini`'s `verify` as `verify=` to `pyeapi.connect`, pyeapi 1.0.4 drops it and uses an unverified context whenever none is given, and `eos_mcp/eapi.py:13-27` patches `ssl._create_unverified_context` process-wide to `SECLEVEL=0` and a TLS 1.0 minimum. So anything that can answer on a device's name or address on port 443 receives the eAPI Basic-auth credentials, and `verify = true` does not change that | MCP07 (credential exposure) | Accepted for the upstream (security review of PR #182), with scope: mitigated in front of it by fathomgate's inventory and the `default:unknown_target` deny (a host the agent names that is not in inventory never gets the credentials) and by the refused `config_path` (M1-35, which could otherwise swap host, credentials and transport); the network path from the eos-mcp host to the devices is the operator's to trust. An upstream fix (pass `context=ssl.create_default_context(cafile=...)` when `verify` is set) was reported privately to the eos-mcp maintainer on 2026-09-25 as GitHub security advisory GHSA-25hx-wvx9-2f2v (draft in the M1-22 handoff) | Operator warning in `profiles/eos-mcp.yaml` hazard 1 and research brief 02 §1.5. Test: `tests/integration/test_eos_mcp.py` `test_passthrough_verify_true_is_not_enforced` (a self-signed fake device is accepted with `verify = true`), and `test_policy_read_only_run_command` (an unknown host opens no connection) |
| Credentials sent to an unknown host by a read: eos-mcp and netdev-ssh-mcp log in to whatever host the agent names with the operator's device credentials (eos-mcp falls back to `[DEFAULT]` in `config.ini`, `config.py:62-70`, over HTTPS with verification off and TLS 1.0, `eapi.py:17-27`; netdev-ssh-mcp takes a free-form `host`), so `get_version` to `core-x.attacker.example` hands them over before any command runs. A policy that left `defaults.unknown_target` unset denied only `WRITE_CONFIG` and `EXEC_ARBITRARY` for an unknown target and let a class-only rule such as `reads-anywhere` allow the read | MCP07 (credential exposure) | Mitigated in `Evaluate` for targets that no provider resolves (M1-37, [ADR 0032](../adr/0032-unset-unknown-target-denies-every-class.md)); enforced by `serve` once the gate is wired (M1-18, M1-19). The hostname-pattern residual is closed (M1-34, ADR 0031): a pattern never makes a target known, so with an operator pattern `^core-`, `core-x.attacker.example` is unknown and the default denies it. Residual, open (owner M1-18): a call with no targets skips the default, and the zero-target refusal is not built yet; for example eos-mcp `daily_brief` given only `tags` (a group selector the upstream expands) reaches the rules with zero targets. Residual, accepted: a policy that sets `unknown_target: allow` | `internal/policy/evaluate.go` step 1: any value but `allow` denies every class with `default:unknown_target`; `load.go` `Parse` fills an unset value in as `deny`. A request with no targets is not affected; the planned normaliser refusal of a zero-target call to a tool that takes a target (`default:bad_arguments`) is open, M1-18. Tests: `TestUnknownTargetDefaults` (unset, `deny` and `allow` across all seven classes, one unknown target alone and among known ones, a `Policy` built without `Parse`, targetless requests, the YAML null forms and the loader refusals); `tests/policy_lint` warns on an explicit `unknown_target: allow` |
| Unmeetable obligation on a held write: `prod-approval.yaml` holds a `WRITE_CONFIG` on a `core` device with `dry_run`, `diff` and `timed_rollback`, but some write tools can do none of them (upa `set_config_commands_and_commit_or_save` sends, commits and saves in one call, `main.py:88-101`) | — (change safety) | Open (security review of PR #150, finding 8). Owner: M3 (`ChangeSafety`, approvals) | Requirement: an obligation the tool cannot meet turns the decision into `deny`, naming the obligation, and never into a silent `allow` or an approved call that runs without it. No approval path exists before M3 (`internal/approval` is not present), so no held call is executed today. In M1 the gate fails closed on an `allow` carrying any obligation other than `redact`, `notify`, `require_ticket` and `canary_first`, including `dry_run`, `diff` and `timed_rollback` (ADR 0026, M1-18); `lab-open.yaml` drops `dry_run` and `diff` from `lab-writes-free` instead of meeting them (ADR 0026 decision 1, PR #154), so its lab writes are forwarded without them until M3 |
| eos-mcp `config_path` local file read and credential substitution: every eos-mcp tool (v1.3.0) takes an agent-supplied `config_path` and returns configparser's error text to the agent (`health_check` `detail`; `get_router_list` as a tool error, since it catches only `FileNotFoundError`, `server.py:115-118`; `Error (<host>): <e>` elsewhere). A file with no `[section]` header has its first line quoted, so single-line secret files come back whole (`~/.git-credentials`, `.pgpass`, `.env`, `~/.netrc`, the `--listen-token-file` file). A file starting with a `[section]` line has every malformed line after it quoted. `/proc/self/stat` gives the parent pid, and `/proc/<ppid>/environ` (one line) returns fathomgate's whole environment: `FATHOMGATE_LISTEN_TOKEN` and every `--upstream-env-pass` credential. The same file also supplies the credentials, transport and `verify` for that call | MCP05, MCP07 (credential exposure) | Classifier side done (M1-35, [ADR 0033](../adr/0033-closed-argument-list-per-tool.md)): no eos-mcp tool names `config_path`, so `Classify` reports it in `UnnamedArgs` with any value, `""` and `null` included. Open until the gate denies it with `default:bad_arguments` (M1-18, wired in M1-19); v0.1.0 `serve` forwards it | `profiles/eos-mcp.yaml` hazard 2 and `refused_args`; research brief 02 §1.5 (`config.py:20-43`, `server.py:22-23`); `TestEOSConfigPathRefused`, `TestRepoProfileArguments`. Until the gate is wired: a `deny` rule for `servers: [eos-mcp]` first in the policy, or run eos-mcp sandboxed so it can read only its own `config.ini` and not fathomgate's `/proc` entries or files |
| Agent-chosen arguments no profile reads: any argument outside a tool's target, command and config parameters reached the upstream unread, so a credential selector (netdev-ssh-mcp `username`), a mode switch (ntunes `send_config` `enter_config_mode=false` sends config lines in exec mode), free text interpolated into a device command (eos-mcp `session_name`) or a parameter added in a later upstream version passed any policy | MCP05, MCP07 | Classifier side done (M1-35, ADR 0033): every profile names each argument of each tool (`args`) or refuses it (`refused_args`); an unnamed argument, or a target, command or config argument that is not a string or list of strings, is reported for the gate to deny with `default:bad_arguments` (M1-18, M1-19). Open for tier 2: compare each upstream's `tools/list` `inputSchema` with the profile (test-engineer) | `internal/classify` `CheckArguments`; profile-schema §2.2. Argument lists must be read from handler code as well as the declared schema. junos `load_and_commit_config` reads an undocumented `config` key (refused) and `timeout` (`jmcp.py:1657, 1660`), which a `tools/list` comparison cannot see. Tier 2 should also fail when a property in `args` has an object schema |
| JSON string in a list parameter: Python FastMCP (`mcp` 1.x, `pre_parse_json`) runs `json.loads` on a string sent for any parameter not annotated plain `str`. So eos-mcp `hostnames: "[\"core-rtr-01\"]"` runs on `core-rtr-01` while fathomgate sees one unknown target named `["core-rtr-01"]`. `hostnames: "null"` makes `daily_brief` run fleet-wide. A `config_lines` sent as a JSON string reads as one payload line, hiding its elements from the M1-36 line check (which now also fails a line starting with `[` or `{`, classification.md section 11) | MCP05, MCP07 | Classifier side done (security review of PR #161, H1): `CheckArguments` reports a mapped argument as malformed when its string value is valid JSON starting with `[`, `n`, `t` or `f`, or (target, group and command arguments only) starts with `[` or `{`. Open until the gate denies it (M1-18) | `internal/classify/normalize.go` `upstreamMayParseJSON`; `TestSecurityJSONStringInListParam`, `TestJSONStringCheckBoundaries`; profile-schema §2.2 |
| Capability-id spoofing on a meta-tool: Meraki `execute_api(capability_id, parameters)` runs whichever Dashboard API operation `capability_id` names, so the tool name says nothing. An agent (or text injected into it) sends an id that fathomgate reads as a listed read while the upstream runs something else, or an id fathomgate has not classified: a case variant (`GetOrganizations`), padding or a trailing newline, a zero-width or no-break space, a look-alike letter (Cyrillic `e`, U+0435), a fullwidth or combining form, a `\u` escape or lone surrogate in the JSON, a duplicate `capability_id` key, a missing, `null`, numeric, list or JSON-string value, or an operation the upstream adds later. The upstream's own read-only claim (`readOnlyHint: true`, a GET-only index and a `get` name-prefix check) is upstream data and untrusted (invariant 7) | MCP03 (confused deputy), MCP05, MCP07 | Mitigated (M1-17, in `Classify` and `internal/gate`): the id is looked up byte for byte in the profile's capability table, with no trimming, folding or normalisation, and anything the table does not list is `EXEC_ARBITRARY` with `class_source: capability_table`; the loader accepts only printable-ASCII table keys with no space, refuses two keys that differ only in case, and requires a meta-tool's own class to be `EXEC_ARBITRARY`. A non-string, list or JSON-looking value is malformed (`default:bad_arguments`); the gate refuses invalid UTF-8 and duplicate keys before classifying, a lone surrogate decodes to U+FFFD and matches nothing, and the proxy forwards the re-encoded object it checked, so the upstream receives the id that was classified. Annotations only raise: `readOnlyHint: false` makes a listed read `EXEC_ARBITRARY`, `readOnlyHint: true` never lowers an unlisted id. The reason and log line name the table, never the id. Residual, accepted: the table's classes are the profile author's reading of the spec's category tags, checked by test but not by running each operation. The upstream version and its SDK binding (which Meraki SDK method a capability id reaches) are not checked at run time: the id set is pinned by `TestMerakiCapabilityTable` against the fixture from `c1d00ea`, and a run-time check is open. `semantic_search` returns text from the upstream's bundled spec (summaries and descriptions), which is untrusted upstream content that can steer the agent (MCP03); it is `INVENTORY_READ`, and its output is not labelled or filtered. `parameters` is refused, so the organisation, network or device a listed read reaches is not policed yet: next row (moved to M2) |
| Meraki `parameters` scope and JSON-keyed secret redaction: Meraki `execute_api` takes `parameters`, an object whose keys the upstream passes to the SDK method as keyword arguments. They choose the organisation, network or device serial a read reaches, plus query options. fathomgate cannot see or police that scope, so it refuses `parameters` today, and only 4 of 493 capabilities can run. Many `READ_CONFIG` capabilities return secrets as JSON values under Meraki keys: SSID `psk`, RADIUS secrets, SNMP communities, `enrollmentString` on `getNetwork` and `getOrganizationNetworks` (security review of PR #196, F1). The current redactor finds none of them (0 hits on `{"psk": ...}`, F5). The decision log line also does not say which capability a call matched (F3). | MCP07 (authorisation), MCP08, credential exposure | Open, M2: moved to M2 by the maintainer 2026-09-25. `parameters` stays refused until a decision record meets four requirements from the security review of PR #196: (1) the path and query keys each capability accepts, taken from the pinned spec, with scalar values only; (2) `organizationId`, `networkId` and `serial` read as targets, so inventory, `unknown_target` and `max_devices` apply; (3) Meraki JSON redaction fixtures passing before `parameters` is named; (4) a per-organisation rate or session cap. Deferred with it (F3): the decision log line names the matched capability key (the table key, never the agent's bytes). The log line is unchanged in M1-17 | `profiles/cisco-meraki-mcp-official.yaml` header (`refused_args: [parameters]`); ADR 0033 notes of 2026-09-25; board task M1-17 note | `internal/classify/normalize.go` `classifyCapability`, `CheckArguments`; `profile.go` `validateTable`, `validCapabilityID`; `internal/gate/gate.go` (raise on the table's class); `profiles/cisco-meraki-mcp-official.yaml`; tests `TestCapabilityClassify`, `TestCapabilityCheckArguments`, `TestParseCapabilityFields`, `FuzzCapabilityLookup`, `FuzzCapabilityTableKey`, `TestMatrixRow18`, and in `internal/gate` `TestMatrixRow18Gate`, `TestCapabilityRawJSON`, `TestCapabilityAnnotations`, `TestCapabilityArguments`; profile-schema §2.5 |
| Agent-supplied Jinja2 runs on the upstream host: junos-mcp-server `render_and_apply_j2_template` renders `template_content` in a plain Jinja2 `Environment` (`jmcp.py:1369-1375` at `75fe90a`), before it reads `apply_config` or `dry_run`. A template such as `{{ cycler.__init__.__globals__.os.popen(...) }}` runs Python there, and can read fathomgate's `/proc/<ppid>/environ`. `lab-open` allowed it as `WRITE_CONFIG` | MCP05 (code execution), MCP07 | Mitigated by class (security review of PR #161, H2): the tool is `EXEC_ARBITRARY` with `never-downgrade`. A policy that allows it allows code execution on the MCP host. It must never become a dry-run candidate in M3 | `profiles/junos-mcp-server.yaml` hazard 1; classification.md section 9 |
| Agent-chosen `port` and fan-out: netdev-ssh-mcp `port` is named, and on a console server or jump host one port can reach a different device than the host policy checked. eos-mcp `max_workers` and ntunes `max_concurrent` are named and uncapped | MCP07 (authorisation), availability | Open (security review of PR #161, M1). Owner: M2 (per-argument value constraints in the profile schema), M4 (fan-out cap). Until then, do not put console servers in the inventory under device roles | ADR 0033 section 6; profile-schema §3 `fanout_params` |
| Answers to upstream elicitation are an argument channel: an upstream that asks the agent for input mid-call gets values the closed list never checks. junos v1.1.1 `add_device` elicits `ssh_key_path` | MCP03, MCP07 | Mitigated in `internal/proxy` (M1-19, decided under ADR 0014, *Notes after acceptance*; security review of PR #161, N1): with a policy loaded, an upstream prompt during a call to a tool whose server has a profile is refused on both paths (`input_required` and `elicitation/create`) and never shown, so no answer reaches the upstream. Live when `serve` loads a policy (M1-20). Residual, accepted: with `--no-policy`, or for a server with no profile, prompts are relayed as in M0 and their answers are not checked. Pin a junos version before validation | ADR 0033 *Notes after acceptance*; `profiles/junos-mcp-server.yaml` hazard 2; ADR 0014 (relayed input requests) |
| eos-mcp free-form `hostname` sends the fleet's eAPI credentials: `get_creds` falls back to `[DEFAULT]` for a host missing from `config.ini` (`config.py:62-70`), so any hostname the agent names receives the username and password over HTTPS with `verify=False` by default and a process-wide TLS context lowered to `SECLEVEL=0` and TLS 1.0 (`eapi.py:17-27`) | MCP07 (credential exposure) | Open (security review of PR #158). Owners: M1-34 (hostname patterns and known targets; done, ADR 0031), M1-18 (gate: unknown-target check before dispatch), M1-37 (new: a policy that leaves `unknown_target` unset must default to `deny`) | `default:unknown_target` denies an unknown host today in `policy eval` (checked: `get_version hostname=evil.example`); `serve` enforces no policy until M1-18. Operators: set `verify = true` per host in `config.ini`. `profiles/eos-mcp.yaml` hazard 1 |
| Fleet-wide run with zero targets: eos-mcp `daily_brief` with neither `hostnames` nor `tags` runs on every host in the upstream's `config.ini` (`server.py:476-478`), while fathomgate sees zero targets, so no `device_roles` or `device_tags` rule and no fan-out cap sees the fleet | MCP07 (authorisation) | Mitigated in `internal/gate` (M1-18, PR #162), wired at `Proxy.dispatch` (M1-19); live when `serve` loads a policy (M1-20). Found by the security review of PR #158 | The gate denies with `default:bad_arguments` a call with zero targets, or with a tag or group selector, to a tool whose profile names a target source and whose profile class is not `INVENTORY_READ` or `LOCAL_ADMIN` (profile-schema section 2.2). Test: `TestZeroTargets`. `profiles/eos-mcp.yaml` hazard 4. The other eos-mcp batch tools run nothing on an empty selection (`server.py:176-178, 296-298, 341-343`) |
| eos-mcp `push_config` session escape: `config_lines` go unfiltered into one eAPI call after `configure session <name>` (`eapi.py:118-143`), so lines such as `end` then `reload now` or `configure` may run outside the session, before the final `abort`, even with `dry_run=True`. The same class of risk for netmiko `send_config_set` (upa `set_config_commands_and_commit_or_save`, ntunes `send_config`, `send_config_parallel`), which leaves configuration mode on `end` or `exit`, and for a Junos `load set` payload. `session_name` and `commit_timer` are interpolated unchecked | MCP05 (command injection), change safety | Mitigated at classify (M1-36; found by the security review of PR #158): every line of every `config_params` element is checked; a control or non-ASCII byte, a first word in the CLI escape list (`end`, `exit`, `configure`, `commit`, `abort`, `do`, `reload`, `bash`, `show` and the rest, with abbreviations), or for junos `load_and_commit_config` in set format a first word outside the set statements, makes the call `EXEC_ARBITRARY` (`class_source: reclassify`). After the security review of PR #170 also: any `;` (NX-OS separator), aliases (`alias`, `cli`), scheduled execution (`event`, `event-handler`, `schedule`, `scheduler`, `kron`, `daemon`, `command`; Junos `event-options`, `scripts`, `extensions`, `execute-commands` in every format), more EOS exec verbs (`ping`, `clock`, `watch`, `terminal`, ...), Junos `top`/`up` with a command, xml `<!`, and a 1024-byte line and 64 KiB payload cap. After round 2: a login `autocommand` in any word position (`line vty 0 15` then ` autocommand reload`, `username netops autocommand reload`; IOS runs it at the next login, and upa, ntunes and netdev-ssh-mcp open a session per call), mitigated with `TestSecurityConfigSessionEscape` and gate `TestConfigSessionEscape`; `;` in a comment line; punctuation around a Junos word. Live when `serve` loads a policy (M1-20). Open: (1) the CLI list is a denylist and EOS configuration mode accepts every exec command, so an EOS exec verb missing from the list still passes `push_config`; the follow-up is an allow-list of top-level configuration words for `push_config` (classification.md 11.5); (2) tier 2 on cEOS proves the device behaviour, including `clock set` and `watch` (M1-28); (3) Junos text-format abbreviations (`system { scr { ... } }`) pending tier 2 on vMX or cRPD (M1-28). Accepted: IOS `menu m command 1 reload` (reachable only through an exec escape or autocommand already caught) and EOS `username ... shell` (changes what later logins land in). `session_name` is refused (ADR 0033); `commit_timer` is an int FastMCP validates | classification.md section 11; `internal/classify/config.go`; `TestSecurityConfigSessionEscape`, `TestSecurityJunosLoadConfig`, `TestSecurityConfigEscapeWords`, `TestConfigLinesThatStayWrites`, gate `TestConfigSessionEscape`. `profiles/eos-mcp.yaml` hazard 3. The class is `WRITE_CONFIG` whatever `dry_run` says. Requirement for M3: a fathomgate `dry_run` driver must never use the upstream's `dry_run=True`, which sends the payload to the device before approval |
| Operator-configured aliases: an alias defined on the device before fathomgate saw it (IOS `alias configure hn do reload`, EOS `alias hn reload now`, NX-OS `cli alias name hn reload`) turns an ordinary-looking config line (`["hn", ""]`) into an exec command | MCP05 (command injection) | Accepted (security review of PR #170, H2). fathomgate cannot see device state at classification. A definition sent through a config tool is `EXEC_ARBITRARY` (`alias`, `cli` are escape words), so an agent cannot create one; operators audit existing aliases on devices behind upa, ntunes and eos-mcp | classification.md 11.2 and 11.5; gate `TestConfigSessionEscape` (the two-call sequence) |
| Persistence and lock-out by configuration: `username ... privilege 15 ... nopassword`, `aaa authorization exec default none`, `management api http-commands`, Junos `set system login user ... class super-user`, `boot system` are ordinary writes that leave a backdoor, lock operators out or change what boots | MCP07 (authorisation), change safety | Accepted by design (security review of PR #170): they stay `WRITE_CONFIG`, held on production by `prod-approval` and allowed on lab devices by `lab-open`. The approver sees them in the diff (M3). Configuration that schedules execution is not in this row: it is `EXEC_ARBITRARY` (classification.md 11.2, 11.3) | classification.md 11.5; `TestConfigLinesThatStayWrites`, `TestSecurityJunosLoadConfig` (super-user row) |
| Shell hosts behind netmiko: netmiko `linux` and generic device types run every line of `send_command` and `send_config_set` as a shell command, so the CLI word lists mean nothing there | MCP05 (command injection) | Accepted, operator guidance (security review of PR #170, note 2): do not put shell hosts in the inventory behind upa or ntunes | classification.md 11.5 |
| Leading `-` in netdev-ssh-mcp `run_ping` and `run_traceroute`: `destination`, `source`, `vrf` and `outgoing_interface` match `[A-Za-z0-9._:/@%-]{1,255}` (`command_safety.go:19-33` at v1.7.1), so one argument can become an option word in `ping` or `traceroute`; the verb is fixed by the server | MCP05 (command injection) | Accepted for M1 (security review of PR #158; T0.53): both tools stay `READ_OPERATIONAL`, and the worst case is an extra ping or traceroute option, not a state change. Owner: M2 (per-argument patterns in the profile schema) | `profiles/netdev-ssh-mcp.yaml` header |
| Loopback family squat: `--listen localhost:P` bound only `127.0.0.1:P`, so another local user bound `[::1]:P`, and an agent configured with `http://localhost:P/mcp`, which tried `::1` first, sent it its bearer token (and the reverse for `--listen [::1]:P`) | MCP07 (auth) | Mitigated (T0.52; H1 in the security review of PR #109, reproduced by the reviewer) | `cmd/fathomgate/listen.go` `parseListenAddr` takes only `localhost`, `127.0.0.1` or `[::1]` (another `127.0.0.0/8` address, bound alone, left `127.0.0.1` and `[::1]` free; security review of PR #112, reproduced on Linux) and `bindLoopback`, after the address asked for, binds the other family's loopback on the same port and serves both; if it cannot be bound for any reason but a missing loopback family, fathomgate exits 1 before the upstream starts, naming the address; a host without that family logs a warning and serves on one. One `listening` line per address; install.md tells operators to paste a URL from it. [ADR 0023](../adr/0023-listener-binds-both-loopback-families.md). Tests: `TestBindLoopbackHoldsBothFamilies` (the regression: with the listener up, neither family can be bound on its port), `TestBindLoopbackRefusesTakenOtherFamily`, `TestServeListenOtherFamilyTaken`, `TestBindLoopbackInjected`, `TestLoopbackFamilyMissingUnix`, `TestLoopbackFamilyMissingWindows`, `TestListenerEndToEnd` (agents on both addresses), `TestServeListenProcess`, `TestParseListenAddr`. CI sets `FATHOMGATE_REQUIRE_BOTH_LOOPBACKS=1` in the Linux, Windows and macOS jobs, so these fail instead of skipping on a runner without IPv6, and greps each job's log for their `--- PASS`. Residuals, accepted: a client whose `localhost` resolves elsewhere than `127.0.0.1` and `::1` is not covered, such as a legacy macOS hosts file with `fe80::1%lo0 localhost`, or a client configured with the machine's host name, which Debian maps to `127.0.1.1`; install.md's advice to paste the `listening` URL, an IP literal, avoids both. While fathomgate is not running, see the next row |
| Port squatting while fathomgate is down or restarting: another local user binds the loopback port (either family) before fathomgate does, and an agent, which has no way to authenticate fathomgate, sends it its bearer token; the token then works against fathomgate once it is back. On Windows the same without a race: binding a wildcard address on the port (`0.0.0.0:P`, or `[::]:P` IPv6-only or dual-stack) while fathomgate runs, then waiting for it to stop | MCP07 (auth), MCP03 (confused deputy) | Split by case (M1-27, [ADR 0029](../adr/0029-remote-listener-tls-and-loopback-authentication.md) decision 4, accepted in part for M1: the Windows exclusive bind; TLS deferred to M2; owner mcp-protocol-engineer). Windows takeover of fathomgate's own addresses: defence in depth. Windows wildcard squat while fathomgate runs: mitigated by design; measured only with same-user sockets; the other-user case, which is the threat (a same-user attacker can already read the token file), is to be measured. Squat while fathomgate is down or restarting, every platform: open until TLS on loopback (M2) | `cmd/fathomgate/listen.go` (`bindLoopback`, `listenTCP`; `runListener` exits 1 when the upstream exits, so a supervisor restarts it) and `serve.go` (the bind before the upstream starts). **Windows, exact address (defence in depth):** both loopback sockets are bound with `SO_EXCLUSIVEADDRUSE` (`listen_windows.go` `bindControl`, set before bind; if it cannot be set, the bind fails and fathomgate exits 1). Microsoft documents that with it no other socket can bind the same address and port, whatever its options ("Using SO_REUSEADDR and SO_EXCLUSIVEADDRUSE"). On Windows 11 build 26200 a same-user takeover with `SO_REUSEADDR` already failed without it (`WSAEACCES`). **Windows, wildcards (mitigated by design; measured only with same-user sockets):** `SO_EXCLUSIVEADDRUSE` covers only the exact address, so after both loopback binds `holdWildcards` binds the port on three wildcards, `0.0.0.0:P` (IPv4), `[::]:P` IPv6-only and `[::]:P` dual-stack (`IPV6_V6ONLY` 1 and 0), without `SO_EXCLUSIVEADDRUSE` and without listening, from non-inheritable sockets that are closed with the listeners. They accept nothing, so agents still reach fathomgate on both loopback addresses and nothing listens off the host. While they are held, another socket's bind of port P on any wildcard or loopback address fails (`WSAEADDRINUSE`, or `WSAEACCES` with `SO_REUSEADDR`). If a wildcard is already held when fathomgate starts, it exits 1 naming the address (`[::]:P (dual-stack), a wildcard address on the same port, cannot be held ...`), before the upstream starts, as on Unix. Measured only with same-user squatting sockets, on Windows 11 build 26200 and on the Windows Server CI runner (security review of PR #156); the other-user case, which is the threat, is to be measured. Tests: `TestBindLoopbackExclusiveWindows` (the option read back on both sockets; binds of both loopbacks and all three wildcards, with and without `SO_REUSEADDR`, fail; agents reach both addresses; closing the listeners frees the wildcards), `TestBindLoopbackRefusesHeldWildcardWindows` (one case per wildcard kind), `TestServeListenWildcardHeldWindows`, `TestHoldWildcardsNotInherited`, `TestBindControlFailsClosedWindows`, `TestBindLoopbackHoldInjected`; the Windows CI job greps each `--- PASS`. **Unix (unchanged):** `listen_unix.go`: `bindControl` and `holdWildcards` are nil. No socket can bind an address and port that a listening socket holds unless both set `SO_REUSEPORT`, which fathomgate never does. Linux also refuses an overlapping wildcard bind, with or without `SO_REUSEADDR` or `SO_REUSEPORT` (`TestBindLoopbackRefusesWildcardLinux`, Linux CI job). macOS and the BSDs refuse an overlapping bind from another non-root user (`in_pcbbind`; not tested, it needs a second account). They allow it from the same user, who can already read the token file. **Open until M2 on every platform:** a squat while fathomgate is down or restarting. The fix is TLS on loopback, which lets an agent that trusts a custom CA refuse a squatter ([ADR 0029](../adr/0029-remote-listener-tls-and-loopback-authentication.md)), or a Unix socket or named pipe protected by permissions (its open question 2). Mitigations today: fathomgate refuses to start when either loopback address, or on Windows any wildcard, is taken on its port, so a squat shows in its log; [install.md](../install.md#remote-agents-over-http) tells operators to have the supervisor restart fathomgate at once; tokens are per client, so one can be withdrawn without the others, by removing its `--listen-token-file` entry (or replacing the file's contents) and restarting fathomgate: token files are read once at startup, so deleting a file while fathomgate runs withdraws nothing |
| Tampered release download: a mirror, proxy or compromised account swaps an archive or `checksums.txt` on its way to a user | MCP04 (supply chain) | Mitigated (release PR #119) | `.goreleaser.yaml` `signs:`: `cosign sign-blob --bundle` (keyless, Sigstore) over `checksums.txt`, which lists every archive and SBOM; the release carries `checksums.txt.sigstore.json`. The Fulcio certificate names `.github/workflows/release.yaml@refs/tags/<tag>` in `fathomgate/fathomgate`, issuer `https://token.actions.githubusercontent.com`, trigger `push`; docs/install.md and each release's notes give the `cosign verify-blob` command with all three pinned. Proven only by the first tag run: snapshot.yaml skips signing |
| Signing identity reachable by test or dependency code: the release job holds `id-token: write`, so code it runs (tests, `go mod tidy`, licence scripts, a compromised module) could mint a token and sign something else as the release workflow | MCP04 (supply chain) | Mitigated (release PR #119, security review finding 1) | `release.yaml`: an unprivileged `test` job (`contents: read`, no `id-token`, `persist-credentials: false`) runs `go test`, the `go mod tidy` diff check, `make vulncheck` and `make licences-check`; the `goreleaser` job `needs: test` and runs GoReleaser with `--skip=before`, so it only builds, packages, catalogues (syft) and signs. Its checkout keeps no credentials, and it has no registry login and no tap token (finding 2); the tag must match `^v[0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z.]+)?$` and the notes reach GoReleaser from a fixed path (finding 3). No workflow reachable from a pull request has `id-token` |
| Tag integrity: someone other than the maintainer creates, moves or deletes a `v*` tag and so starts or rewrites a release | MCP04 (supply chain) | Mitigated | Repository ruleset `release tags` on `refs/tags/v*` (creation, update, deletion, non-fast-forward; bypass: organisation admins only). Tags are annotated and signed by procedure (`git tag -s`, `.claude/commands/release.md` step 7); the ruleset does not enforce the signature |
| Unsigned or single-arch artefacts on other channels: a container image or Homebrew formula published without a signature, or a formula pointing at another tag | MCP04 (supply chain) | Accepted for v0.1.0 (binaries only) | `.goreleaser.yaml`: `dockers` `skip_push: true` (image built and checked by snapshot.yaml, not pushed) and `brews` `skip_upload: true` (`fathomgate/homebrew-tap` does not exist). Each comes back with multi-arch, cosign signing and, for the tap, a token scoped to the tap only |
| Release job without a human gate: anyone who can push a `v*` tag starts a job that mints the signing identity at once, with no second person to approve it | MCP04 (supply chain) | Open (security review of PR #119, finding 4). Owner: maintainer | No GitHub deployment environment exists (`gh api repos/fathomgate/fathomgate/environments` lists none on 2026-09-25). Fix: create a `release` environment with a required reviewer in repository settings, then add `environment: release` to the `goreleaser` job in `release.yaml`. Until then the `release tags` ruleset (admins only) is the only gate |
| Audit signing key substitution: another local user plants a symbolic link, junction or hard link at the key path, or a key file of their own, or finds the key group-readable, and fathomgate signs checkpoints with a key they hold, so they can rewrite the log and re-sign it (security review of PR #112, T0.55) | MCP08 | Mitigated (M1-09, ADR 0028) | `internal/audit` `LoadKey` reads through `internal/secretfile.Read`, the check behind listen token files: `O_NOFOLLOW` and `O_NONBLOCK` on Unix, `FILE_FLAG_OPEN_REPARSE_POINT` on Windows, then on the open descriptor or handle a regular (disk) file, not a reparse point or directory, with one link, owned by the current user, mode `0600` or `0400` and no macOS extended ACL, or a protected DACL whose allow entries name only the owner and `SYSTEM`. No group-owned key; no flag to skip. The error names the path and the check, never the key. Tests: `TestLoadKeyUnixMode`, `TestLoadKeyUnixRefusesSymlink`, `TestLoadKeyUnixRefusesDirectory`, `TestLoadKeyUnixRefusesOtherOwner` (root), `TestLoadKeyRefusesHardLink`, `TestLoadKeyDarwinRefusesExtendedACL`, `TestLoadKeyWindowsDACL`, `TestLoadKeyWindowsRefusesSymlink` and `TestLoadKeyWindowsRefusesOtherOwner` (privileged, required in CI), `TestKeygenOutputPassesLoadKey`, and `internal/secretfile` tests. Residual, accepted: root or an administrator can still read or replace the key; FreeBSD and illumos NFSv4 ACLs are not read (row 38) |
| Verifying with the signing key: `audit verify --key` accepted the private key and derived the public half, so a verification needed the secret, an auditor had to be handed it, and a check that passes with the signer's own file proves nothing to a third party | MCP08 | Mitigated (M1-09, ADR 0028) | `LoadPublicKey` accepts exactly one `PUBLIC KEY` PEM block; any private key block is refused with `audit.ErrPrivateKey` before parsing, and `audit verify` exits 2 with `--key must be the public key (<path>.pub); a verifier never needs the signing key`. Tests: `TestLoadPublicKeyRefusesPrivateKey`, `TestKeyRoundTrip`, `TestAuditVerifyAndKeygen`. Residual: that the `.pub` file is the right key is the operator's to establish |
| Private key hidden in a public key file: `pem.Decode` skips text it cannot parse, so a private key (as text, as base64 or as a corrupted `PRIVATE KEY` block) placed before the `PUBLIC KEY` block of a `.pub` passed `audit verify --key`, and an auditor handed that file held the secret without knowing it (security review of PR #155, L1) | MCP08 | Mitigated (M1-09 fix round) | `LoadPublicKey` refuses with `audit.ErrPrivateKey` any file containing `PRIVATE KEY` anywhere, before decoding; `onePEMBlock` (both loaders) then needs `-----BEGIN ` exactly once, with only white space before it and after the END line. Tests: `TestLoadPublicKeyRefusesPrivateKey` (private block before and after, leading private-key text, a corrupted private block before the public one, bare base64 before it, text after it) |
| Named pipe at the public key path: `audit verify --key <fifo>` blocked forever waiting for a writer (security review of PR #155, L2) | MCP08 (availability) | Mitigated (M1-09 fix round) | `LoadPublicKey` opens with `O_NONBLOCK` on Unix (with `SECURITY_SQOS_PRESENT \| SECURITY_IDENTIFICATION` on Windows) and refuses anything but a regular file on the open descriptor. `secretfile.Read` already opened with `O_NONBLOCK`. No owner or mode check on the public key (ADR 0028). Tests: `TestKeyLoadersRefuseFIFO`, `TestReadFIFO`, `TestLoadPublicKeyRefusesDirectory` |
| Impersonation through a secret file path on Windows: a key or token path naming a named pipe (`\\.\pipe\...`, or a UNC path to one on another host) lets the pipe server impersonate fathomgate's user during `CreateFile`, before the disk-file check refuses the handle (security review of PR #155, L3) | MCP07 | Mitigated (M1-09 fix round) | `internal/secretfile` (`secretfile_windows.go`) and `LoadPublicKey` open with `SECURITY_SQOS_PRESENT \| SECURITY_IDENTIFICATION`: the server can identify the caller but not impersonate it. The handle is then refused as not a disk file. Not tested with a live pipe server; the flags are pinned by review |
| NFSv4 ACLs outside macOS: on FreeBSD, illumos and NFSv4 mounts an ACL entry can give another user access the mode bits do not show, and `internal/secretfile` reads only the mode there (security review of PR #155, L4) | MCP08 | Accepted, limitation | `internal/fileacl` reads extended ACLs on macOS only; Linux POSIX ACLs show in the group bits, which are checked. Keep keys and token files on a local filesystem without NFSv4 ACLs, or check them with `getfacl` / `ls -v`. Also in SECURITY.md |
| Parent directories not checked: `internal/secretfile` checks the final file (no final symbolic link, owner, mode, links, ACL or DACL) but not the directories on the path, so another user who can write to one of them can rename or replace the key file between loads (security review of PR #155, N1) | MCP08 | Accepted | Whatever they put there must still pass the checks, owned by fathomgate's user and owner-only, which another unprivileged user cannot create. So they can make the load fail, or move another of fathomgate's own owner-only files into place (which then fails to parse as a key, or is a key fathomgate's user already holds), but they cannot substitute a key they hold. Intermediate symbolic links are followed. Keep keys in a directory only the proxy user can write |
| Multi-line, control-character and look-alike injection through a free-form command tool: the agent sends `show clock` followed by a line break and `conf t`, `copy run start`, `relo` plus a blank line, or `tclsh`; whitespace collapsing hid the break, so the call was downgraded to `READ_OPERATIONAL` while netmiko ran every line on the device (security review of PR #150, critical) | MCP05 | Mitigated (M1-16, PR #152) | `internal/classify/command.go` `classifyCommand`: any control byte other than tab and any non-ASCII byte (NEL, U+2028, U+2029, C1, no-break and zero-width spaces, fullwidth look-alikes) is `EXEC_ARBITRARY`, checked before whitespace is collapsed; commands over 1024 bytes too; `Normalize` trims only spaces and tabs and keeps empty elements. The blocklist carries CLI abbreviations (`conf t`, `wr`, `rel`, `relo`, `copy`, `tclsh`) so the flattened payloads fail as well. Since M1-39 (PR #185) the anywhere blocklist runs as a word-set and word-pair lookup (`blocked`) instead of an unanchored regular expression; the expression stays in the tests as the specification. Tests: `security_test.go` (`TestMultiLineInjectionIsExecArbitrary`, 12 line-break variants through 5 free-form tools; `TestFlattenedInjectionIsExecArbitrary`), `TestDowngradeNeverAccepts`, `TestCommandLengthCap`; for the M1-39 rewrite, `TestBlocklistWordsMatchRegexp` and `FuzzBlocklistWords` (`blocked` against the expression), and the whole-function differential `TestClassifyCommandMatchesOld` and `FuzzClassifyCommandMatchesOld` (`classifyCommand` against a frozen copy on the pre-M1-39 expressions, class and check id, seeded with every string literal of the package's tests and the review's hand cases: separators, `conf`/`t` whitespace variants, near-miss abbreviations, `${IFS}`, `$'x'`, vertical tab, U+00A0, U+2028, fullwidth look-alikes). Gate cases (ADR 0035, M1-33): the line-break, Unicode-separator, NEL, vertical-tab, look-alike, `relo`, `tclsh` and `;` inputs through upa, eos-mcp and ntunes in `policies/examples/read-only.gate.test.yaml`. |
| Config dump through a short form or a file read: `show ru`, `show tec`, `show derived`, Junos `show sys rol 1` (the previous configuration with `$9$` secrets), a bare FortiOS `show`, NX-OS `show file bootflash:backup.cfg` (saved configs on bootflash) or `show diff rollback-patch checkpoint cp1 running-config` (config diff lines with password hashes) classified `READ_OPERATIONAL`, and so allowed where a policy denies `READ_CONFIG` (security reviews of PR #150 and #152, high) | MCP05 | Mitigated (M1-16, PR #152) | `internal/classify/command.go` `isConfigRead`: the second word (keywords include `file` and `diff`; `show file systems` and `show d` over-match, accepted) (and the third after an abbreviation of `system`) is a config read when it is a prefix of a config keyword or a keyword is a prefix of it; a bare `show`, `get` or `display` is `READ_CONFIG`. Over-matching into `READ_CONFIG` is accepted. Tests: `TestConfigRead`, `TestConfigDumpAbbreviations`, `TestExitCriterion3`. Before test-matrix row 5 is validated, check that vJunos accepts `show sys rol 1` through the `<command>` RPC. Gate cases (ADR 0035, M1-33): `show ru`, `show tec`, `show sys rol 1`, `show file bootflash:` and a bare `show` through upa, eos-mcp, netdev-ssh-mcp and junos-mcp-server, and `show running-config` through ntunes `send_command`, in `policies/examples/read-only.gate.test.yaml`. |
| FortiOS `show` (and operational commands on any vendor that print secrets: IOS and NX-OS `show snmp community`, `show key chain`, `show crypto isakmp key`) classified `READ_OPERATIONAL`, because the classifier does not know the vendor; FortiOS `show vpn ipsec phase1-interface` and `show user local` carry `ENC` secrets (security review of PR #152, medium) | MCP05 | Accepted, open until the vendor reaches `Classify` (needs an ADR; owner policy-engineer) | classification.md section 2 and section 9 record it. Mitigation: M2 redaction MUST run on every tool result, whatever the class and whether or not a rule carries `redact` (invariant 4). |
| Shell-quoted option injection: on a server that runs `ping` or `traceroute` on its own host, `ping 1.1.1.1 "-f"`, `'-f'`, a backslash-escaped `-f`, `ping {-f,1.1.1.1}`, `ping 1.1.1.1 [-]f`, a glob, `~root`, `$'...'`, `$HOME` or `${IFS}` rebuild an option the leading-dash check never saw (security review of PR #152, low) | MCP05 | Mitigated (M1-16, PR #152) | `internal/classify/command.go` `hasShellMeta` (a byte loop since M1-39, PR #185; before, the expression `shellMeta`) refuses `"`, `'`, backslash, `{`, `}`, `*`, `?`, `[`, `]`, `~` and a `$` not at the end of a word, as well as pipes, redirects and chaining; `leading-dash` refuses any later word starting with `-`. Tests: `TestShellQuotedOptionInjection`, `TestDowngradeNeverAccepts`; for the rewrite, `TestShellMetaMatchesRegexp` and `FuzzShellMeta` (against the old expression on any string) and the whole-function differential `TestClassifyCommandMatchesOld` and `FuzzClassifyCommandMatchesOld`. Gate cases (ADR 0035, M1-33): the quoted, escaped, brace, bracket, `${IFS}` and `$'...'` forms of `ping -f` in `policies/examples/read-only.gate.test.yaml`. |
| Upstream era mislabelled from the version in its handshake answer: after the ADR 0018 restart or go-sdk's own fallback, an upstream answers the 2025-11-25 `initialize` request with 2026-07-28, go-sdk accepts it, and the version-only rule labelled a session opened with the initialise handshake `era=stateless`, which M1 writes into the audit event and the decision log line | MCP08 (audit accuracy) | Mitigated (M1-06, PR #151) | `internal/proxy/era.go`: `handshakes`, a sending middleware on the upstream's client, marks each session whose `initialize` request was answered; `upstreamEra` labels such a session stateful whatever version it answered, and only a `server/discover` session at a stateless version stateless. `upstream.era` is set once at connect. Tests: `TestUpstreamEra`; the two 2026-07-28 rows of `TestDiscoverProbe` and `TestRestartedStdioUpstreamAnswering2026IsStateful` (real stdio child) failed with the version-only rule (checked, then reverted), and since M1-32 expect the hybrid refusal instead (renamed `TestRestartedStdioUpstreamAnswering2026IsRefused`). The upstream still sets the logged `protocol` value. Spec: profile-schema 8.4 |
| Server-initiated input from an upstream labelled stateless: an upstream connected through `server/discover` (era label stateless) sends `elicitation/create` during a `tools/call`; go-sdk accepts it in either era, so a control that read the era as "this upstream cannot prompt" would be wrong | MCP03 | Mitigated (M1-32) | Found by the security review of PR #151 (F1). fathomgate refuses it: `upstreamElicitation` (`internal/proxy/input.go`) checks `upstream.handshake`, set once at connect from how go-sdk opened the session (not `upstream.era`), and refuses an `elicitation/create` from an upstream connected with `server/discover` before any other check on an attributed call, with or without a policy and with or without a profile (M1-19's refusal under a policy covered profiled servers only). The upstream gets a JSON-RPC error and the agent the refusal naming the reason and ADR 0008; the prompt is not shown. With zero or several calls in flight the attribution refusal applies, which also shows nothing. Sampling is refused for every upstream (no sampling capability declared). The field starts unset, so a prompt before connect finished would be refused. Test: `TestDiscoveredUpstreamElicitationRefused` (a raw `elicitation/create` from a 2026-07-28 upstream to a 2025 and a 2026 agent, no policy; the 2025-11-25 upstream row still relays), which fails with the check removed (checked, then reverted). Spec: profile-schema 8.2 and 8.4 |
| Hybrid upstream session: `initialize` answered with 2026-07-28 when fathomgate asked for 2025-11-25; the session has a handshake and a session, but go-sdk adds the stateless `_meta` self-description to every request because it keys that on the version | MCP03 | Mitigated (M1-32) | Maintainer ruling on PR #151. The `handshakes` sending middleware (`internal/proxy/era.go`) reads the requested version from the `initialize` request go-sdk sent and refuses an answer naming a later one, or any stateless version even if equal to the request (no handshake session may carry one): it returns an error in place of the answer, so go-sdk never sends `notifications/initialized` and closes the session, and it first cancels the connect attempt, which kills the upstream's process tree at once. `fathomgate serve` exits 1 with an error naming both versions (the answered one quoted, clipped and escaped) and ADR 0008. No flag; never retried (a refusal is an answered probe, even if the ADR 0018 bound runs out during the close). Tests: `TestHandshakeMiddleware` (later, older and equal answers, the equal stateless one refused), `TestHybridRefusalIsNotRestarted` (the bound runs out after the refusal and before go-sdk's close: one transport, no restart; fails without the cause guard, checked, then reverted), the two hybrid rows of `TestDiscoverProbe` (go-sdk's fallback and the ADR 0018 restart; no initialised notification reaches the upstream) and `TestRestartedStdioUpstreamAnswering2026IsRefused` (real stdio child that ignores EOF and SIGTERM: refused, killed well inside go-sdk's 5-second close grace, both processes reaped; fails with the kill removed, checked, then reverted). Spec: profile-schema 8.4 |
| Annotation raise re-run as a downgrade: a read tool annotated `readOnlyHint: false` was reclassified as if its profile said `EXEC_ARBITRARY` and its commands re-checked, so a `READ_CONFIG` tool with `command_params` and `show version` became `READ_OPERATIONAL` and a policy that denies `READ_CONFIG` allowed it (security review of PR #162, H1) | MCP03 | Mitigated (M1-18) | `internal/gate/gate.go` `classify`: a raise sets `EXEC_ARBITRARY` and never re-runs the downgrade; it only makes a class stricter. Tests: `TestAnnotationRaiseNeverLowers`, `TestAnnotationsOnlyRaise`. Spec: classification.md section 4 |
| Unlisted upstream tool (rug pull): an upstream adds a tool after the profile was written (`eos-mcp.run_command_v2 {"hostname": "totally-unknown", "command": "reload"}`); with no profile entry it had zero targets, so the unknown-target default and `max_devices` never saw its host (security review of PR #162, M1) | MCP03 | Mitigated (M1-18) | The gate refuses with `default:bad_arguments` any call to a tool the profile does not list exactly that carries an argument (ADR 0033 section 2); `unnamed_args` in the log line. Tests: `TestUnlistedToolWithArguments`, `TestExactToolLookup`. Open while a server has no profile at all: nothing is checked, and `serve` warns (M1-20) |
| Unmet obligation forwarded: an `allow` carrying an obligation fathomgate cannot carry (a typo, `commit_confirmed`, or any name in a policy built in Go without `Validate`) was forwarded because the gate only stopped `dry_run`, `diff` and `timed_rollback` (security review of PR #162, M2) | — (change safety) | Mitigated (M1-18) | `internal/gate/gate.go` `forwardable`: an allow-list (`redact`, `notify`, `require_ticket`, `canary_first`); anything else is `cannot run` and is not named to the agent. Tests: `TestObligationsAllowList`, `TestObligationsFailClosed` |
| Parser differential, target spelling, group selectors and zero targets: a duplicated JSON key or invalid UTF-8 read differently by fathomgate and the upstream; a target such as `lab-x@core-rtr-01`, ` core-rtr-01`, `CORE-rtr-01`, `127.1` or `"null"` (which FastMCP `json.loads` into `None`, the whole fleet) that fathomgate and the upstream resolve differently; a `tags` selector or an empty `hostnames` that the upstream expands to devices fathomgate never counted (security reviews of PR #152, #154, #158 and #161) | MCP05, MCP07 | Mitigated (M1-18) in `internal/gate`, wired at `Proxy.dispatch` (M1-19), live when `serve` loads a policy (M1-20). The proxy also forwards the re-encoded object the gate checked, never the agent's bytes, so a parser differential below the top level cannot reach the upstream either; arguments over 64 KiB are refused before the gate | Parse: one object, UTF-8, no repeated top-level key. Targets: hostname or IP literal exactly as sent, exact-case inventory match, no pattern-only match (ADR 0031), no group selector and at least one target on a tool that reaches devices. Tests: `TestParse`, `TestValidTargetName`, `FuzzValidTargetName`, `TestAttackerHostNames`, `TestZeroTargets`. Spec: profile-schema section 2.2 Gate cases (ADR 0035, M1-33): duplicate key, trailing data, not an object, the per-call caps, `lab-x@core-rtr-01`, a leading space, a trailing dot, a zero-width space, `hostnames: "null"`, a tag selector and zero targets in `policies/examples/*.gate.test.yaml`. |
| Log injection through the decision line: agent-chosen argument names (`unnamed_args`, `malformed_args`) and validated target names reach the Info log line | MCP08 | Mitigated by `slog` escaping (text and JSON handlers quote control characters) and by caps (8 names, 64 bytes each) | `internal/gate/record.go`; `TestUnlistedToolWithArguments` checks the cap. The proxy writes the line through the operator's logger with no handler of its own (M1-19); `TestDecisionLine` and `TestRealGateLogInjection` (`internal/proxy`) check that a name with a newline stays on the one line |
| A failing or overloaded gate: a panic in `Decide` or in an inventory resolver, or an arguments object large enough to make parsing, classification and the log line expensive, forwarding a call nobody decided or stopping the proxy | availability, MCP07 | Mitigated (M1-19) | `internal/proxy/gate.go`: arguments over 64 KiB are `default:bad_arguments` before `Decide`; a panic is recovered as `default:internal_error` (the Error line names the panic's kind, never its value) and the proxy keeps serving; a verdict is forwarded only when it says so. Tests: `TestGateArgumentCap`, `TestGatePanicDenies` |
| Session counters dodged: concurrent calls of one principal both passing `max_devices` on the same count; a device named twice counted twice; a 2025-era agent resetting its count by closing its session, opening another or having it evicted (T0.57); one principal doubling its cap by using both eras (security review of PR #167, M1) | MCP07 (authorisation) | Mitigated (M1-19) | Counted per principal over HTTP for both eras and per process on stdio (ADR 0026 decision 4 as amended by the maintainer 2026-09-25); no count belongs to a session, so session churn cannot reset it. The read, the decision and the update of one key run under one lock; a device already touched is counted once. Since M1-39 (PR #185) that takes one `Decide`: the proxy passes `seam.CallInfo.Counted`, a read of the key's remembered names valid only during the synchronous `Decide`, and the gate takes those targets off `devices_touched` (never below 0); a name past the 4,096 remembered is not `Counted`, so the count still errs high. Residual, accepted: two principals are two counts; an operator who wants one cap gives an agent one token. Tests: `TestGateCountersSurviveSessionChurn`, `TestGateCountersPerPrincipal`, `TestGateCountersSerialised`, `TestGateCountersStdio`, `TestRealGateThroughProxy`, `TestCountedTargets`, `TestGateDecidesOnce`, `TestTouchedCap` |
| Per-call amplification, decision DoS: one call at the 64 KiB argument cap carried about 1,900 commands or 3,300 target names, and the gate classified or resolved each, 5 to 18 ms of CPU under the counter key's lock, so one agent could slow every decision of its principal and load the proxy (M1-23 findings) | MCP05 (availability) | Mitigated (M1-39, PR #185) | `internal/gate/args.go` `overCaps`, before classification: more than 64 commands or 256 target names and group selectors (counted as sent, nested arrays flattened, comma parts and repeats counted) is `default:bad_arguments` with fixed text that names only the cap (profile-schema 2.4). The command checks run without unanchored regular expressions (about 5.5 µs per KiB of command), and the proxy runs `Decide` once. A call at the caps takes about 1 ms at p50 in `Decide` and in the proxy stage. Residual: a call still costs parsing its 64 KiB (about 0.4 ms and 0.4 MB) before the count. Tests: `TestPerCallCaps`, `TestCountValues`, `TestPerCallCapConstants`, `TestDecideOverhead` and `TestDispatchOverhead` (strict steps; `KnownOverBudget` empty) |
| Configuration load errors disclosing file contents: a policy or inventory with a typo printed the surrounding lines, including a device password beside it, to fathomgate's stderr and the client's log (security review of PR #171, M1) | — (credential exposure) | Mitigated (M1-20, PR #171) | `internal/yamlstrict`: every policy, inventory and profile decode error is reduced to `[line:column]` and the message, and the message is dropped when it quotes a value; `TestNoFileContentInErrors` and one FAKE-canary test per loader. Residuals: a secret typed where a key belongs and shaped like one (lower-case snake_case) or like a vocabulary word (UPPER_SNAKE) is still named; validation errors quote schema values (ids, names, patterns) by design (profile-schema 8.3) |
| Server without a profile under `--policy`: the fallback classifier found no target, so the unknown-target default and `max_devices` never ran and reads went anywhere the upstream connects (security review of PR #171) | MCP07 (authorisation) | Mitigated (M1-20, PR #171; orchestrator decision, ADR 0027 note of 2026-09-25) | `cmd/fathomgate/serve_policy.go` gives such a server an empty profile: a call with any argument is `default:bad_arguments`; `TestNoProfileServerDeniesArguments` |
| Integrity of the policy, inventory and profile files: another local user who can edit them can allow anything (security review of PR #171, M4) | MCP07 (authorisation) | Mitigated (M1-20, PR #171), with residuals | `internal/configfile`: exit 2 when a file or the `--profiles` directory is writable by group or others, or owned by someone other than the effective user or root (Unix; a POSIX ACL named-user write shows as `g+w` through the mask), or grants write to anyone but the owner, SYSTEM and Administrators (Windows, every offending account named); checked on the opened file. Residuals: the parent directories are not checked, so a user who can write one can swap the file between runs (rename, symbolic link, hard link, junction); the check trusts the effective user and root (Unix) or the current user, SYSTEM and Administrators (Windows), so it is only as good as those accounts. Follow-up (M2): a parent-directory check in the manner of OpenSSH `StrictModes`. Operators are told to keep the files in a directory only they and the administrators can write (docs/install.md) |
| Gated serve not exercised in CI against a real upstream: pass-through jobs alone would not catch a gate that forwards (security review of PR #171, M2) | — (assurance) | Mitigated (M1-20, PR #171); one part open | `TestServePolicyEndToEnd` (HTTP both eras, and stdio as a child process); tier 2 `test_policy_gate.py` runs `serve --policy read-only.yaml` over stdio in front of netdev-ssh-mcp v1.7.1 and the fake device, whose session log shows the denied calls opened no SSH session. Open: a conformance leg with a policy (it needs a test profile for the suite's server); owner M1-28 |
| Issue tracker abuse through the hygiene job: an outsider titles an issue like a task (`M1-22 ...`) to have the job relabel or close it; a title carries a workflow command (`::set-env`, `::add-mask`), control, format or line-separator characters, or Markdown and HTML into the log and job summary; a pull request triggers the job to get its `issues: write` token; a board edit or a bug closes many issues at once | MCP04 (supply chain), MCP07 | Mitigated (M1-38, security review of PR #175) | `.github/workflows/issue-hygiene.yaml`: triggers are push to `main` on `docs/milestones/**`, a weekly schedule and `workflow_dispatch`, never `pull_request` or `pull_request_target`; `permissions: {}` at the top, `contents: read` and `issues: write` on the job, `GH_TOKEN` on one step, checkout without persisted credentials, actions pinned by SHA, uv pinned by version and PyYAML installed from a hash-checked requirements file; the script's output runs between `::stop-commands::` markers with a random token. `tools/status/issues.py`: only issues whose author association is `OWNER`, `MEMBER` or `COLLABORATOR` are managed (others are reported, never touched); titles lose Unicode `Cc`, `Cf`, `Zl` and `Zp` characters before printing and sit in a code span in the summary; only labels `.github/labels.yml` defines are set (exit 2 otherwise); a task id on two boards is refused; more than `--max-close` closures (default 5) closes nothing and exits non-zero, and a push run closes only tasks whose state changed in that push. `tests/unit/test_status_issues.py` |
| Policy test suites that prove something other than what serve enforces: a `*.test.yaml` case took its class and targets as given, so the PR #152 injection inputs and the PR #154 names could not be written as cases and test-matrix rows 3, 4 and 6 were proved only at the policy level; `policy eval --profile` re-implemented part of the gate and skipped the parse checks, the per-call caps, the group and zero-target refusals and the annotation raise (security reviews of PR #152 and #154) | — (assurance) | Mitigated (M1-33, [ADR 0035](../adr/0035-policy-test-cases-run-the-gate-path.md)) | A gate case runs a raw `tools/call` through `internal/gate` `Explain`, the steps `Decide` runs for the proxy (`TestExplainMatchesDecide` pins that the verdicts are equal), with the embedded profiles or a `--profiles` set loaded by `internal/configset` as `serve` loads it, and an inventory read as `serve --inventory` reads it. `policy eval --profile` decides through the same function; `TestPolicyEvalMatchesGateCases` checks it against every shipped gate case. Load errors stop a case that could pass for the wrong reason: a server with no profile, `class` or `targets` on a gate case, no `expect.rule`, arguments over 64 KiB. Residual: gate cases are tier 1; a row is validated only against its real upstream (M1-28). |
| A policy test suite as injection text: a `*.test.yaml` that carries the PR #152 inputs, or a crafted case name, argument name, `policy:` path or `inventory:` path, prints control, escape or bidi characters to the terminal or CI log running `fathomgate policy test`, or echoes an argument value (a pasted secret). Before the follow-up to PR #197 (L1), an `inventory:` path such as `nothere\e[31m.yaml` reached the error raw through `configset.ReadInventory` and the operating system's error, and the `policy:` path through `policy.Load` | — (terminal and log injection) | Mitigated (M1-33, ADR 0035; L1 in the follow-up to PR #197, R1 in its re-review) | `internal/policytest` messages and `policy test` output carry no argument value other than validated target names (the gate's `targets`, which are hostnames or IP literals). One helper, `internal/termsafe`, quotes a case name, argument name, target name, rule id, trace note or path that is not printable ASCII, for the CLI and the runner alike. Every path that can come from a file or a directory listing is quoted where it enters a message (the test file, its `policy:` and `inventory:` paths, `internal/configset`'s profile and inventory paths), `internal/configfile` no longer repeats the raw path an operating-system error carries, and every CLI error printer (`fail`, `failTo`, `serve`, `inventory lint`) passes the whole error through `termsafe.Text`, which escapes every C0 control, line feed and tab included, DEL, C1, U+2028, U+2029 and the bidirectional formatting characters, so a line break a parser echoes (the re-review of PR #199, R1: an inline inventory `match: |` holding `(` and a forged `3 cases, 3 passed, 0 failed`) cannot forge a line; `internal/configfile`'s fix commands travel beside the error (`configfile.Hint`) and are printed after it, each line escaped too. An invalid hostname pattern (`inventory: roles[i]`) and a profile's server key and tool names are also quoted where they enter a message. Load errors go through `internal/yamlstrict`, which never quotes the file. `policy eval` uses the same helper. Tests: `TestNoArgumentValueInOutput` (both paths), `TestPolicyTestOutput`, `TestFailEscapesControls`, `TestFailSink`, `TestNoForgedLines` (every parser error path), `TestReadInventory`, `internal/termsafe` `TestText`, `FuzzText`. |
| A policy test file that expands without bound: YAML anchors and aliases let a small file repeat a large value, and a case name is printed on every PASS and FAIL line (security review of PR #197, L3: a 425 KB file with a 200 KB anchored name and 3000 aliases took 40 s and printed 600 MB) | — (availability of the CI job and the terminal) | Mitigated (M1-33, the follow-up to PR #197; owner policy-engineer) | `internal/policytest` reads a test file and its policy through a 16 MiB cap (`configset.MaxFile`), refuses an anchor or alias anywhere in a test file before it is decoded (nothing expands), and caps a case name at 256 bytes (`MaxCaseName`). Test: `TestTestFileCaps` (the reviewer's shape is refused in well under five seconds with a short error). |
| The files `policy test` and `policy eval` read without the `configfile` checks: a `*.test.yaml` file, the policy it names, and `policy eval --policy` | — (integrity) | Accepted (M1-33 follow-up to PR #197) | These configure a check, not an enforcement: nothing they say reaches a device, and `serve --policy` reads the policy it enforces with the checks. Everything a gate case decides with that `serve` also loads is read as `serve` reads it: the profile set (`--profiles` or `eval --profile`, through `internal/configset`) and an inventory given as a path (`inventory:` or `eval --inventory`). The test file and the policy are size-capped (16 MiB). |

## OWASP MCP Top 10 mapping (rows above)

| OWASP MCP | Rows |
| --- | --- |
| MCP03 tool poisoning (and confused deputy through relayed prompts) | Server-initiated input from an upstream labelled stateless (mitigated, M1-32), hybrid upstream session (mitigated, M1-32), progress spoofing, progress token scope, cross-principal `requestState` replay, prompt schema round-trip, stateful upstream prompt misattributed across agent sessions, prompt for a call that ended normally, relayed prompt on the wrong stream, unsolicited `inputResponses`, port squatting (while fathomgate is down: open until M2 TLS; Windows wildcard squat while it runs: mitigated by design, measured only with same-user sockets, other-user case to be measured, M1-27), capability-id spoofing on a meta-tool (mitigated, M1-17) |
| MCP05 command injection through output (and availability) | Multi-line, control-character and look-alike injection (M1-16), config dump through a short form (M1-16), FortiOS `show` classified `READ_OPERATIONAL` (accepted, open until the vendor reaches `Classify`), shell-quoted option injection (M1-16), Listener answering for a dead upstream, progress spoofing, progress flood, notification queue blocked, resource exhaustion by a client that does not read, prompt schema round-trip, cross-principal refusal of stateful prompts via the shared orphan entry (accepted residual, T0.44), prompt availability from M3, eos-mcp `push_config` session escape (mitigated at classify, M1-36; EOS denylist completeness and tier 2 on cEOS open, M1-28), operator-configured aliases (accepted), shell hosts behind netmiko (accepted), leading `-` in netdev-ssh-mcp ping and traceroute (accepted for M1), JSON string in a list parameter (classifier side done, M1-35), agent-supplied Jinja2 on junos (mitigated by class), capability-id spoofing on a meta-tool (mitigated, M1-17) |
| MCP07 insufficient authentication and authorisation | DNS rebinding and cross-site requests, listener token handling, listen token exposed on the host (client side open, documentation only, T0.33), loopback family squat, go-sdk compatibility switches, session id exposure, cross-principal `requestState` replay, target alias drift (open, M1), pattern-resolved target, reads and writes (mitigated, M1-34, ADR 0031), upstream transport hazards accepted (upa over stdio only), impersonation through a secret file path on Windows, port squatting (while fathomgate is down: open until M2 TLS; Windows wildcard squat while it runs: mitigated by design, measured only with same-user sockets, other-user case to be measured, M1-27), eos-mcp free-form hostname credentials (open, M1-18), fleet-wide run with zero targets (mitigated in `internal/gate`, M1-18; live once wired, M1-19), credentials sent to an unknown host by a read (mitigated in `Evaluate`, M1-37; pattern residual closed, M1-34; zero-target residual, M1-18), eos-mcp `config_path` file read (classifier side done, M1-35; refused by `internal/gate`, M1-18; live once wired, M1-19), agent-chosen arguments no profile reads (classifier side done, M1-35), agent-chosen `port` and fan-out (open, M2), answers to upstream elicitation (open, M1-19), capability-id spoofing on a meta-tool (mitigated, M1-17; `parameters` scope open), Meraki `parameters` scope and JSON-keyed secret redaction (open, moved to M2 by the maintainer 2026-09-25) |
| MCP04 software supply chain | Tampered release download, signing identity reachable by test or dependency code, tag integrity, unsigned or single-arch artefacts on other channels (accepted for v0.1.0), release job without a human gate (open, maintainer) |
| MCP08 missing or tamperable audit | Listen token exposed on the host (row 38: since T0.52 the audit key and log are also refused with a macOS extended ACL); audit signing key substitution, verifying with the signing key, private key hidden in a public key file, named pipe at the public key path, NFSv4 ACLs outside macOS, parent directories not checked; the checkpoint-key custody rows in SECURITY.md; upstream era mislabelled from the version in its handshake answer (mitigated, M1-06). Note for M1-19: the audit event and the decision log line must record both the upstream's `protocol` and its `era`, and never treat the era as a capability; Meraki matched capability key not in the decision log line (open, M2) |
