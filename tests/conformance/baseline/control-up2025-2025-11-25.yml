# Expected failures: control-up2025 leg (shim -> go-sdk v1.6.1
# everything-server -http, always stateful, no fathomgate), --requirements
# 2025-11-25. Format and exit rules: tests/conformance/README.md.
#
# Empty on purpose: the 2025-only upstream passes every scored 2025-11-25
# check on its own HTTP handler, so every entry in
# fathomgate-up2025-2025-11-25.yml is a difference fathomgate makes. This leg
# has no 2026-07-28 run (run.sh skips it): a 2025-only server cannot answer a
# stateless agent with nothing in between.
server: []
