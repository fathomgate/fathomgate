# Expected failures: control leg (shim -> go-sdk everything-server -http
# -stateless=true, no fathomgate), --requirements 2026-07-28. Format and exit
# rules: tests/conformance/README.md. An entry that starts passing fails the
# run.
#
# Empty since T0.32. The 12 HTTP-transport entries this file held measured
# relay.py's HTTP behaviour, not go-sdk's; with go-sdk serving HTTP itself
# they all pass (go-sdk v1.8.0 conformance/baseline.yml has no server
# entries either), which also shows the shim transparent for this revision.
server: []
